# 此文件告诉 gcloud 哪些文件不需要上传到 Cloud Build
# 类似 .dockerignore，但作用于 gcloud builds submit 的上传阶段

# 依赖（Cloud Build 内会重新 npm ci）
node_modules/

# Next.js 构建产物（Cloud Build 内会重新 build）
.next/
out/
build/

# 环境变量（敏感！通过 Secret Manager 注入，绝不上传）
.env
.env.local
.env*.local
.env.production
.env.development

# 编辑器/系统文件
.DS_Store
.idea/
.vscode/
*.pem

# 日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript 缓存
*.tsbuildinfo

# Prisma 生成文件（会在 npm ci 时重新生成）
lib/generated/

# 本地文档（不影响运行）
pic/

# Git
.git/
.gitignore

# 本地数据库备份（不上传）
# 必须锚定到根目录：.gcloudignore 用 gitignore 语义，未锚定的 `backups/` 会匹配任意层级，
# 把 app/api/backups/ 这条备份管理 API 路由一起从上传清单里剥掉，导致生产 /api/backups 404
# （2026-08-02 实测：gcloud meta list-files-for-upload 里没有该路由，Cloud Run 日志全是 404）。
# .gitignore 里本来就是 /backups/，这里之前漏了斜杠。
/backups/

# Claude Code 的工作树/配置，含整个应用的多份副本（实测 1190 个文件），不应进构建上下文
.claude/
