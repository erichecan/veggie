# 主流程对比分析

> 分析日期：2026-05-02
> 对比基础：用户描述的 14 步主流程（见 WORKFLOW-PRD.md）

---

## 一、主流程自身：有无错误或遗漏？

**结论：流程整体合理，无明显逻辑错误，有以下 3 处值得注意：**

| # | 问题 | 说明 |
|---|------|------|
| 1 | 司机分配时机有歧义 | 你描述"司机在报价单阶段（步骤 3）通过快速编辑分配"，但实际配送行程（Trip）是在波次生成之后的独立步骤。两者不矛盾，但需明确：报价单的 `deliveryBatch` 字段是"批次备注"，不等于真正分配了某位具体司机。正式司机分配发生在创建行程时。 |
| 2 | 分货步骤的主体 | 你描述"司机核验货物 → 分货员确认"，但分货员界面（/sorter）是系统中独立操作的。司机核验通常发生在出发前（行程详情页），两者在时间上可以衔接但操作在不同页面。建议在流程中明确各自的入口。 |
| 3 | 退货审核后的会计流程 | 你提到退货审核后"会计列表显示异常标记"，这个联动在代码层面是通过 `returns` 字段挂在 Trip 上的，会计页面目前按订单维度展示，对退货的可见性需确认。 |

---

## 二、与产品需求（ODOO-REPLICATION-PLAN.md / DEMO-GUIDE.md）的比对

### ✅ 一致的部分

| 主流程步骤 | 产品需求 | 状态 |
|-----------|---------|------|
| 步骤 0：商品/客户/价格表配置 | ODOO-REPLICATION-PLAN 明确要求商品模型、客户模型、价格表 | ✅ 一致 |
| 步骤 1：运营代客建单 | ODOO-REPLICATION-PLAN 中有 place-order 作为核心入口 | ✅ 一致 |
| 步骤 4：生成拣货波次 | DEMO-GUIDE 第 4 步"生成拣货波次"完全吻合 | ✅ 一致 |
| 步骤 5：拣货员执行拣货 | DEMO-GUIDE 第 6 步拣货员流程 | ✅ 一致 |
| 步骤 6：分货员分货 | DEMO-GUIDE 第 7 步分货员流程 | ✅ 一致 |
| 步骤 8-9：司机配送 | DEMO-GUIDE 第 9 步司机流程，含签收/异常 | ✅ 一致 |

### ⚠️ 存在差异的部分

| 主流程步骤 | 产品需求（原始文档） | 差异说明 |
|-----------|-------------------|---------|
| 步骤 3：报价单确认机制 | DEMO-GUIDE 原流程中没有"报价单"概念，餐馆直接自助下单 | **流程升级**：你的流程是运营主导的 B2B 报价流程（更接近 Odoo），比原 DEMO 流程复杂，这是有意的升级而非冲突 |
| 步骤 7：司机分配时机 | DEMO-GUIDE 步骤 8"运营创建配送行程"发生在分货完成后 | ⚠️ **轻度冲突**：你描述"司机在报价单阶段就分配好了"，但 DEMO-GUIDE 的原设计是先完成拣货/分货再建行程。现有代码（trips/page.tsx）也是独立创建行程，支持在分货前后均可创建。需确认是否要求"先分配司机再拣货"或"拣货后再分配司机"。 |
| 步骤 10：退货/换货单 | ODOO-REPLICATION-PLAN 中提到退货但未定义完整的审核流程 | ⚠️ **产品需求不完整**：审核状态（PENDING_REVIEW / APPROVED / REJECTED / SETTLED）是代码自行实现的，原产品文档没有明确定义这套审核流程 |
| 步骤 11：会计对账 | ODOO-REPLICATION-PLAN 提到"现金核销"，DEMO-GUIDE 没有会计步骤 | ✅ 代码已实现，但产品文档描述简略 |
| 步骤 12：发票生成 | ODOO-REPLICATION-PLAN 明确写"Invoice 模块：从销售单生成，Open/Paid 状态 — 完全缺失" | ✅ **代码已实现**（invoices/page.tsx），ODOO-REPLICATION-PLAN 的"缺失"描述已过期 |

---

## 三、与代码实现的比对

### ✅ 代码已实现，与流程一致

| 主流程步骤 | 代码实现位置 | 状态 |
|-----------|------------|------|
| 步骤 1：运营建单 | `place-order/page.tsx` | ✅ 已实现 |
| 步骤 2：报价单列表 | `quotations/page.tsx`（含 tabs、搜索、分页） | ✅ 已实现 |
| 步骤 3：确认报价单 | `quotations/[id]/page.tsx` → Action → Confirm Quotation → `apiPut({ status: 'CONFIRMED' })` | ✅ 已实现 |
| 步骤 4：生成波次 | `waves/page.tsx` → 待生成面板 → 合并生成，同时创建 Trip 占位 | ✅ 已实现 |
| 步骤 5：拣货 | `/picker` 路由存在 | ✅ 路由已存在 |
| 步骤 6：分货 | `/sorter` 路由存在 | ✅ 路由已存在 |
| 步骤 7：创建行程/分配司机 | `trips/page.tsx` → 4 步向导（时段→订单→司机→确认） | ✅ 已实现，司机从 DRIVER 角色用户列表选择 |
| 步骤 8-9：司机配送 | `/driver/trip/[id]` 路由存在 | ✅ 路由已存在 |
| 步骤 10：退货审核 | `returns/page.tsx`，含 PENDING_REVIEW/APPROVED/REJECTED/SETTLED 状态 | ✅ 已实现 |
| 步骤 11：会计对账 | `accounting/page.tsx`，含批量核销、现金核对 | ✅ 已实现 |
| 步骤 12：发票 | `invoices/page.tsx`，含从订单生成发票、draft/posted/paid 状态 | ✅ 已实现 |

### ❌ 代码与流程有冲突

| # | 主流程描述 | 代码实际行为 | 冲突说明 |
|---|-----------|------------|---------|
| 1 | **步骤 3：从报价单列表快速编辑（分配司机）** | `quotations/page.tsx` 没有内联快速编辑面板；点击行只是跳转到详情页 | 列表页无快速编辑功能。如需"从列表直接改某字段"，需要新增内联编辑行为 |
| 2 | **步骤 3：确认后停留在报价单列表** | `quotations/[id]/page.tsx` 的 `handleConfirm()` 确认后跳转到 `/classic/operator/orders/${order.id}`（订单详情页），不是回到报价单列表 | 确认后的跳转目标与你预期的不一致 |
| 3 | **步骤 3：deliveryBatch 字段 = 司机** | `deliveryBatch` 字段是自由文本输入框，标签是"Sales Team"，placeholder 是"1 pm Umair"；没有从用户列表选择司机的 UI | 这只是一个文本备注，不是真正的司机关联。真正的司机分配发生在步骤 7（创建行程时） |
| 4 | **步骤 4：波次生成触发场景** | `waves/page.tsx` 的待生成面板显示 `status === 'CONFIRMED'` 的订单（正确）；但需要"勾选 ≥ 2 个订单"才能点"合并生成波次"按钮 | 如果某天只有 1 个订单，**无法**生成波次（按钮不出现）。这是个潜在的操作限制，流程中没有提及 |
| 5 | **步骤 7：行程创建时拉取哪些订单** | `trips/page.tsx` 的 Step 2 拉取 `status === 'confirmed' \|\| 'wave_assigned'` 的订单，即允许在波次生成前后都创建行程 | 行程的创建不依赖分货完成，分货与行程创建没有强制顺序 |

### ⚠️ 代码有但流程未提及（潜在遗漏）

| # | 代码功能 | 说明 |
|---|---------|------|
| 1 | `waves/page.tsx` 单个订单生成波次 | 点击列表中单个订单右侧"生成 →"只是跳转到订单详情，没有直接生成单订单波次的逻辑 |
| 2 | 行程创建时订单状态更新 | `trips/page.tsx` 生成行程后提示"订单状态将更新为'配送中'"，但实际更新在 API 层（`/api/trips/[id]/route.ts`）处理，流程中没有提到这个状态变更 |
| 3 | `accounting/page.tsx` 的退货可见性 | 会计页面显示的是订单维度的数据，退货记录挂在 Trip 的 `restaurants[].returns[]` 上，会计页面通过 `orderReturn` 字段显示退货标记，但需确认这个字段是否已在订单数据中设置 |
| 4 | 报价单详情的「Create Invoice」按钮 | `quotations/[id]/page.tsx` 有"Create Invoice"按钮，仅在 `isSalesOrder` 时激活；但发票生成的主入口在 `invoices/page.tsx`，两条路径并存 |

---

## 四、需要修改/确认的问题清单

优先级从高到低：

| 优先级 | 问题 | 建议行动 |
|--------|------|---------|
| 🔴 高 | 报价单确认后跳转到订单详情而非回到报价单列表 | 在 `quotations/[id]/page.tsx` 的 `handleConfirm()` 修改跳转目标 |
| 🔴 高 | 只有 1 个订单时无法生成波次（按钮不出现，需 ≥ 2 个） | 确认是否允许单订单生成波次；若允许，修改 waves/page.tsx 的判断条件 |
| 🟡 中 | 报价单列表无快速编辑功能（无法从列表直接改字段） | 确认是否真的需要；若需要，为列表行添加内联编辑面板 |
| 🟡 中 | `deliveryBatch` 字段是文本框，不是真正的司机选择器 | 确认报价单阶段的"分配司机"是文本备注还是要从用户列表选择实际司机 |
| 🟢 低 | 行程创建与分货完成没有强制顺序约束 | 确认是否需要"分货完成后才能创建行程"的校验 |
| 🟢 低 | 会计页面退货标记的数据来源需确认 | 验证 `orderReturn` 字段是否已正确写入订单数据 |
