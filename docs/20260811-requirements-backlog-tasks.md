# 需求任务台账 · 20260811

> **本文件是进度的唯一真相，对话不是。**
> 每个周期从「读本文件」开始，不从「我记得上次做到哪」开始。
> 需求原文与现状徽章见 `preview/20260811-requirements-checklist.html`。

- 任务总数：47（需求）+ 11（前置；Z4/Z5/Z6 周期 1，Z7/Z8 周期 2，Z8 周期 3 拆 Z8a/Z8b，Z9 周期 7，Z10 周期 13）= **58**
- 完成：**45 / 54 条打勾条目**（W1 全部 13 条已完成；另有 W3 4 条被阻塞、W4 3 条本期不做，不计入）
- 当前批次：**W2 主体进行中**，…/H2/H3/C4 已完成 → 下一条 **C5**（电子签名验收）
- 未完成 9 条：C5~C10（6）+ Z2（等域名）+ I1（依赖 Z2）+ Z11（等财务确认）

---

## 执行环境（实测 20260811）

| 项 | 结论 |
|---|---|
| ⚠️ 重启 standalone 服务 | **必须按端口 kill**：`kill -9 $(lsof -nP -iTCP:3002 -sTCP:LISTEN -t)`。standalone 会把进程名改成 `next-server`，`pkill -f "standalone/server.js"` **永远匹配不上** —— 周期 3~7 里的「重启服务」全是空操作，一直在拿旧进程验证。杀完务必核对新 PID |
| dev server | :3001 指向本机 `veggie_test`（:3000 上跑的是另一个项目 `localrank`，与本项目无关——周期 1 台账里那句「已在 :3000 运行」是错的，已更正） |
| ⚠️ **构建 standalone** | **必须用 `npm run build:standalone`**，不能只 `npm run build`。Next 不会把 `.next/static` 与 `public` 拷进 standalone 产物，漏了这一步整页 `ChunkLoadError`，浏览器里啥也验不了 —— 周期 25 之前反复记的「页面视觉未验」全是这个原因，不是路由问题。改完代码的完整节奏：`npm run build:standalone` → 按端口 kill → 重启 → 核对新 PID |
| ⚠️ 登录接口限流 | 同 IP 约 50 秒冷却。连着跑多个走 HTTP 的测试脚本时，脚本之间要 `sleep 45~50`，否则登录失败会被误读成功能坏了 |
| ⚠️ 测试数据必须自身守恒 | 造夹具时不能直接塞 `qtyOnHand` / 不能让两个 wave 挂同一批订单 —— `db:validate` 会报违例，而那不是产品缺陷。周期 25、26 各踩了一次。期初库存要**连 StockMove 一起写** |
| ⚠️ RBAC 快照 | 新增接口会让两个测试红（`可达性矩阵与快照一致`、`旧 token 逐格相同`）。**不得手改快照**，走 `npx tsx scripts/rbac/update-parity-baseline.ts` + `npx tsx scripts/audit/save-reachability.ts`。若「旧 token」那条仍红，那是**真问题**（部署后未重登的人会 403），要去 `lib/role-access.ts` 补旧路径白名单，不是刷快照 |
| 测试框架 | `node --test --import=tsx tests/*.test.ts`，已有 20+ 测试文件 |
| ⚠️ 数据校验 | **`npm run db:validate` 打的是 `.env.local`（Neon 冻结副本），不是测试库**。校验测试库要写全：`npx tsx --env-file=.env.test scripts/validate-data.ts`（打 Neon 要跑 5 分钟以上还查错了库） |
| ⛔ **prisma 命令默认打 Neon** | `prisma.config.ts` 会 `dotenv` 加载 `.env.local`（= Neon 冻结生产副本）。裸跑 `npx prisma db push` 就是打生产副本！必须 `set -a; . ./.env.test; set +a;` 前置（已设的环境变量优先，dotenv 不覆盖）。另注意本项目的 `prisma db push` **不支持 `--skip-generate`**，写了会退化成打印用法帮助（看着像成功，其实什么都没干）|
| ⚠️ 全新库缺主数据 | 除视图外，**RBAC 角色权限**（Z7）与**采购品类分组**（F1）也都只存在于一次性脚本/数据迁移里，`db push` 一律跳过。三者现已全部接进 `npm run db:bootstrap`（第 2/3/5 步）。**新功能若依赖某张「只被脚本填过」的表，务必同时接进 bootstrap**，否则私有化部署上线即是死功能 |
| ⚠️ 全新库缺视图 | `db push` **只同步表，不建视图/扩展**。缺 `v_lot_daily_cost` → 所有 `/api/analytics/*` 500。重建库后必须跑 `npm run db:sql-objects`（已接进 `db:bootstrap` 第 2 步）|
| ⚠️ 建单夹具 | 借用既有客户下单会撞「客户信用冻结 403」——1324/1331 是月结，只要有一张逾期 POSTED 发票就被挡。造夹具请**自建 `paymentTerm:'cash'` 的客户**，顺带让条数断言不被历史单据搅乱 |
| `.env.local` → Neon `ep-icy-rice` | **149,874 单 / 1,337,568 行 / 1,605 客户**；最后订单 07-21，最后操作日志 **08-04** → 迁移后冻结的旧生产副本，非活库 |
| `.env` → Neon `ep-holy-mud` | 凭证失效（AuthenticationFailed） |
| `.env.test` → Neon `ep-late-morning` | 凭证失效（AuthenticationFailed） |
| 本机 PostgreSQL / Docker | **均无** |

> ⛔ **写库红线**：Z1 已于 20260811 关闭 —— 写操作一律打向本机 `veggie_test`（`.env.test`）。**任何情况下不得写 `.env.local` 指向的 Neon 旧生产副本。**
> 只读走查不受此限。

---

## 队列总览

| 批次 | 含义 | 条数 | 可否立即开跑 |
|---|---|---|---|
| **W0** | 前置：解除后续批次的阻塞 | 10 | ✅ 8 条完成；Z2 等客户给域名、Z9 硬停止 |
| **W1** | 只读走查 / 纯文档 / 纯前端，**不写库** | 13 | B3 ✅ E3 ✅ B2 ✅；余 6 条可做；A1/A2/B1/D2 四条走查 ⛔ 等 **Z9** |
| **W2** | 需要写库（schema 变更、下单、收货、盘点） | 30 | ✅ **已解锁**（Z7 关闭，带鉴权 API 恢复 200） |
| **W3** | 被用户决策阻塞 | 4 | ⛔ 等客户答复 |
| **W4** | 本期不做（APP 相关） | 3 | — |

---

## W0 · 前置

> **执行顺序（周期 6 重排）**：Z1 ✅ → Z3 ✅ → Z8a ✅ → B3 ✅ → E3 ✅ → B2 ✅ → **Z7（头号阻塞，挡住 W2 全部 30 条 + Z8b + I1/I2）** → Z4 → Z5 → Z6 → W1 余下 → W2 → Z2
> Z8b 卡住只影响 W1 那四条 UI 走查；其余 9 条不碰浏览器，继续推进不受影响。

- [x] **Z1 起本机 PostgreSQL 测试库** —— 完成 20260811，commit `e5c5793`
      结果：PostgreSQL 17.9（本机已装，无需安装）+ 库 `veggie_test` + `.env.test` 指向 `127.0.0.1`，驱动自动推断为 `pg`
      验收达成：52 张表与 schema 的 52 个模型一致 ✓；种子写入 1677 商品 / 1324 客户 / 8 账号 ✓；`db:validate` 11 项不变量全绿 ✓；`npm test` 384 通过 0 失败 ✓；连接串确认 localhost ✓
      ⚠️ **偏离验收标准一处**：`prisma migrate deploy` **未能使用**。69 个历史迁移无法在全新库上重放（`20260419_decimal_partner_indexes` 整个事务中止），这是项目已知问题（`migrate dev` 撞 shadow DB 同一根因）。改用 `prisma db push` 从 schema 直建。对测试库而言可接受（不需要迁移历史保真），但**这意味着「迁移能否在空库重放」至今无人验证过** → 见 Z6。
      ⚠️ 已知限制：1677 个商品 **库存全为 0**（新格式 CSV 不带库存列）→ 见 Z5

- [x] **Z4 `db:seed:events` 修复** —— 完成 20260811，commit `5ae4ffc`
      **更正原判断**：登记为「幂等性问题」不准确 —— 这是**首次运行就必坏**，事件种子从来没跑完过
      根因：两个字段踩同一个模式——把固定标记串写进带唯一约束的列
        · `Customer.externalId`：8 个供应商共用 `'seed-evt-vendor'` → 第 2 个撞 P2002
        · `Order.externalRef`：全部订单共用 `'seed-evt'` → 第 2 张单撞 P2002
      改法：标记语义靠前缀保留（`<marker>-<n>`），清理/计数/断言三类读取端一并从等值改前缀匹配，**共 7 个使用点**——漏一个就会「清理漏删」或「断言统计不到」，不报错但结果错
      验收达成：首次运行产出 210 订单 / 124 发票 / 116 收款 / 14 采购单 / 131 批次 / 1455 流水，8 项内建守恒断言全过 ✓；连跑第二遍产出逐项一致 → 幂等成立 ✓；`db:validate` 11 项不变量全绿（**这次是有真实数据的全绿，不是空表的**）✓；`npm test` 399 通过 0 失败 ✓

- [x] **Z5 库存底数（期初余额口径）** —— 完成 20260811，commit `9148fb2`
      落的是待决策 9 拍板的口径：不追溯补历史流水，起算日之前作期初余额，之后严格走流水
      产出：`ensureOpeningStock()`（`prisma/seed-events/inventory.ts`）+ CLI `scripts/seed/ensure-opening-stock.ts`（带 localhost 闸门）
      与既有 `ensureNonNegativeStock` **并存而非替换** —— 前者从流水分组表出发只覆盖已有流水的商品（事件跑完防负库存），后者从 Product 出发 LEFT JOIN 才够得着「一条流水都没有」的 1548 个商品。合并会改变事件种子的产出
      验收达成：有库存商品 **129 → 1677** ✓；`db:validate` 11 项不变量保持全绿 ✓；二次运行补足 0 个（幂等）✓；`npm test` 399 通过 0 失败 ✓
      关键设计：写完流水后调 `recomputeOnHand`，`qtyOnHand` 由流水汇总而来 —— **E3 查出的病根就是这两者脱钩，这里绝不能再造一遍**
      可追溯：期初流水标记 `sourceRef='OPENING-BALANCE'`、日期取起算日 **20260805**（droplet 切换日），与业务流水 `EVT-PO-*` 完全可区分
      ⚠️ 起算日的最终取值仍建议与客户确认（若其有明确的系统启用日，改 `OPENING_DATE` 一个常量即可）

- [x] **Z6 迁移链能否空库重放** —— 完成 20260811，commit `4be4412`
      **结论：不能，且不是某个迁移写错了 —— 是缺一个初始迁移**
      证据：空库上第一个迁移即失败。Prisma 报「current transaction is aborted」掩盖了真因，用 psql 直接跑才看到首个真错 `relation "ProductTemplate" does not exist`。核实下来 ProductTemplate / Product / Customer / Order / User **均未被任何迁移创建过**；69 个迁移里只有 18 个含 `CREATE TABLE`，全是 20260419 之后新增的表
      影响：日常增量部署**不受影响**（droplet 上是搬过去的存量库）；但**私有化部署从零起步、灾备重建、CI 起测试库**三种场景会直接卡住
      产出：`scripts/db/bootstrap-fresh.ts`（`npm run db:bootstrap`）+ `docs/20260811-migration-baseline-gap.md`
      验收达成：按验收标准的第二个分支「明确记录迁移链只支持增量、重建走 db push」——已写进文档并固化成脚本 ✓；端到端实测全新空库产出 53 表 / 19 角色 / 1677 有库存商品 / 210 订单 / 113 发票，`db:validate` 11 项全绿 ✓
      **刻意不做 baseline/squash**：标准修法要求对生产库执行 `migrate resolve --applied`，若只把 `0_init` 合进主干而没先在生产 resolve，**下一次 push main 会把生产部署搞挂**。这是生产影响操作，留给能同时协调生产库的窗口
      → 给部署文档的一句话：**从零部署不要用 `migrate deploy`，用 `npm run db:bootstrap`**

- [ ] **Z2 修复生产发信（两层都坏）**
      验收：Resend 真 key 写入密钥源；发件域在 Resend 完成验证；实际发出一封密码重置邮件并收到
      产出：密钥配置 + `lib/email.ts` 发件域
      依赖：需客户提供已验证域名或授权验证 `veggiesupply.ie`
      阻塞：I1（密码测试验收）
      备注：Secret Manager 中 `VEGGIE_RESEND_API_KEY` 当前是字面量 `"placeholder"`；即便换真 key，`noreply@veggiesupply.ie` 在 Resend 未验证仍会 403

- [x] **Z3 确认各角色测试账号可登录** —— 完成 20260811，commit `0f731f2`
      结果：**9/9 账号登录成功**（API 实测，dev server :3001 指向 `veggie_test`，已用 `pg_stat_activity` 证实连的是本机库而非 Neon）
      途中补掉两个种子缺陷：① SALES 无可登录账号（新增 `sales@veggie.com`）② 所有种子账号 `roles[]` 为空数组，等于强制走回退路径，多角色场景测不出来
      顺带确认：登录限流有效（连续失败后返回 `RATE_LIMIT` 并给出冷却秒数）
      ⚠️ **浏览器登录未通过 → 另立 Z8**：curl 全通，但浏览器里表单按原生 GET 提交（跳到 `/enter?`），说明客户端未水合

- [x] **Z7 RBAC 铺底** —— 完成 20260811，commit `d95e328`
      **根因**：RBAC 的角色与权限点不在 seed 里，而是写成**数据迁移**（部署链路 `push main → migrate deploy` 不跑 seed）。而历史迁移链无法在空库重放（Z6），重建测试库只能走 `db push` —— 它只同步 schema、**跳过全部迁移**，三张表因此是空的
      **为什么后果这么重**：`encodePermissions([])` 返回的是**定长全零串**，而 `hasBitmap()` 只判断该串是否非空 → 系统认定「此人有位图且零权限」→ 所有带鉴权 API 403、所有受保护页面弹走。表现得像登录坏了
      产出：`scripts/rbac/apply-data-migrations.ts`（幂等 + 只允许打本机的安全闸门）
      验收达成：19 角色 / 181 权限点 / 20 条绑定 ✓；7 个角色账号登录后位图**全部非零** ✓；`GET /api/orders` 除 driver 403 外均 200 ✓（driver 的 403 是**正确行为**，证明权限在真正区分而非一律放行）✓
      ⚠️ **「受保护页面可进入」未达成** —— 但不是权限问题了（无 token 访问已能正确弹到 `/enter`），是新暴露的 Z9

- [x] **Z8b 受保护页面弹回 `/enter`** —— 根因确认为 Z7，随 Z7 一并解决 20260811
      Z7 补齐权限后，症状消失：无 token 访问正确弹到 `/enter`，带 token 不再被弹回
      两次错误判断留档：周期 3 判定「中间件取不到 cookie、走 `!token` 分支」是错的 —— 当时凭「响应无 `Set-Cookie`」排除了验签失败分支，却漏了**权限拒绝分支同样不设 cookie**。据此做的两个修复（对齐 JWT_SECRET、改用 standalone server）从一开始就不对症
      **教训**：多条分支产生相同可观测输出时，「排除了其中一条」≠「定位了另一条」。当时应直接给中间件加日志或逐分支构造输入，而不是靠响应头反推

- [x] **Z9 页面路由 307 自循环** —— 完成 20260811，无代码改动（是启动参数问题）
      **根因：我自己加的 `HOSTNAME=127.0.0.1`。** 设了它之后 Next 认定的自身 origin 与请求 origin 不一致，next-intl 的 rewrite 被序列化成**绝对 URL** 而非相对路径，Next 判定为跨域 → 降级成 redirect → 自己跳自己
      定位方法：拿**生产站点做对照**——同一份代码，生产 `/enter` 返回 200 且 `x-middleware-rewrite: /zh/enter`（相对），本地返回 307 且 rewrite 是 `http://localhost:3002/zh/enter`（绝对）+ 多一个 `location`。差异只在 rewrite 头的形态，顺着这条查到启动参数
      ⚠️ **起 standalone 服务不要设 `HOSTNAME`**：`PORT=3002 node .next/standalone/server.js` 即可
      验收达成：`/enter` 200 ✓；4 个受保护页面全 200 ✓；无 token 正确弹回 `/enter` ✓；**浏览器真实登录成功并跳转到报价单列表** ✓
      连带验掉三项欠账：
        · A4 的 Source 列 —— 列表里 `RE-260811-003/004` 显示绿色「客户自助」徽标，筛选下拉选 PORTAL 后 19 条 → 2 条 ✓
        · B3 的 `?` 浮层 —— 分组正确、⌘S/⌥N/⌘Enter/⌘P 四条齐全、Mac 符号正常、Esc 可关 ✓
        · A3 的页面视觉 ✓
      **并因此揪出一个真 bug**：详情页一打开就 500（React #310），见下条

- [x] **Z10 详情页 React #310（B3 引入）** —— 完成 20260811，commit `c93a341`
      `useHotkeys` 被放在两个详情页的 `if (loading) return …` **之后** → order 未加载时不调用、加载后才调用 → hook 数量变化 → 白屏 500
      **三道关卡全都拦不住**：typecheck 通过（类型合法）、`next build` 通过（运行时才违反）、`npm test` 通过（无渲染测试）。只有真在浏览器打开才会崩
      这正是 B3 提交时如实标注「`?` 浮层浏览器实测欠着」所对应的风险——Z9 一解除就撞上了
      修法：两页的 `useHotkeys` 上移到所有提前 return 之前，原位留注释

---

## W1 · 可立即开跑（不写库）

- [x] **B3 快捷键抽象为公共组件** —— 完成 20260811，commit `8e98c0d`
      **对原判断的更正**：开工前查清了，「键盘逻辑散在 9+ 组件」这个说法不准确。那 20 处全是输入框内的局部处理（Enter 跳下一格、方向键选下拉项、Tab 回绕），行级编辑早已由 `OrderLineEditor` 统一。真实情况是**页面级快捷键这一层压根不存在**，所以本次是建立它而非搬运既有代码
      产出：`lib/hotkeys.ts`（纯函数层）+ `components/shared/use-hotkeys.tsx`（hook + `?` 浮层）+ 两个详情页接入 + `tests/hotkeys.test.ts`
      接入：报价单 ⌘S/⌥N/⌘↵/⌘P，销售单 ⌘S/⌥N/⌘P，两页均支持 `?` 唤出、`Esc` 关闭
      验收达成：typecheck 0 错 ✓；`npm test` 401 项 399 通过 0 失败（新增 15）✓；`next build` 编译通过、102 静态页正常 ✓；原有键盘行为未改动 ✓
      ⚠️ **未达成一项**：`?` 浮层的实际视觉与交互**没在浏览器里验过** —— 详情页是受保护路由，被 Z8b 挡着进不去。Z8b 解除后必须补这一步，不能因为构建通过就当它对

- [x] **A1 常购清单 UI 交互走查** —— 完成 20260811，commit `61425bd`
      产出：`docs/20260811-portal-frequently-ordered-walkthrough.md`（浏览器真实点击）
      验收达成：可点元素**全部有反馈、无死按钮** ✓（一键复购 toast + 角标、数量按「上次 N 个」预填正确、购物车增减删、已加购变数量步进器）；下架商品不可加购 ✓；**空状态存在** ✓（「没有找到 xxx 相关商品」）；问题已记入报告 ✓
      本次顺手修掉 P2：购物车行副标题 `" ·  · €22.00"`（分隔符无条件拼接，而 spec/uomName 多数为空）
      **P1 值得优先处理**：下架商品从常购清单**静默消失且名额不递补** —— 接口先切 TOP 8 再返回且只给 productId，前端拿已过滤 ACTIVE 的目录 join，join 不到就丢。实测下架第 1 名 → 清单 8 条变 7 条无提示。客户看到「我常买的东西不见了」而界面不解释原因
      其余记录未改：P3 商品名截断无 tooltip（同名不同规格因此无法区分，本库真实存在）、P4 目录一次渲染 1677 张卡片致整页 75863px、P5 排序不带时间衰减、P6 €0.00 商品可加购（需确认生产是否也有）

- [x] **A2 账期与定价显示走查** —— 完成 20260811，commit `d89bb8a`
      产出：`docs/20260811-portal-pricing-tax-walkthrough.md`
      验收达成：账期 `weekly` → 「周结」一致 ✓；门户显示价 €2.10 → 落单成交价 2.10 一致且来源可追溯 ✓；税前/税后口径 —— **原本完全无标注，本次已修** ✓
      **台账原先记的那条风险成真了，而且比预想的严重**：不只是「显示不含税价」，而是
      · **P0 门户下单的订单行税率 100% 为空** —— 实测 PORTAL 来源 5/5 行 NULL，INTERNAL 1444/1446 有值。根因：`lib/server-pricing.ts` 号称「服务端权威定价」但只有价格权威，税率完全取自客户端提交，而门户不传该字段。后果是**开票按 0 计税**，13.5% 税率下一单 €418.55 就少算 €56.50
      · **P1 三处不标注** —— 商品卡片/购物车合计/订单详情合计均无标注，订单详情甚至取了 `taxRate` 却从不渲染
      修法：税率回落到商品档案（该值早已查出只是没用），量纲由新增 `normalizeTaxRate()` 收口 + 5 个单测；购物车与订单详情统一显示「合计（不含税）/ 税额 / 含税应付」，下单按钮改含税额
      仍需关注：€0.00 商品可下单（与 A1 的 P6 同源）；历史门户单税率仍为 NULL，是否追溯回填需业务确认

- [x] **B1 重复/缺货提醒交互走查** —— 完成 20260811，commit `de89dbd`
      产出：`docs/20260811-quotation-duplicate-shortage-walkthrough.md`
      验收达成：三种场景全部通过 ✓；**指明具体行与商品** ✓（横幅写明商品名与重复次数，行级另有标记——重复行 🔁、缺货行整行标红）；**明确能否继续保存** ✓（实测 Save 成功不阻断，文案「或保留现状手动调整」已说明；设计为告知而非阻断，合理）
      提醒质量高：缺货口径标注「基于可承诺量」，低库存（ATP<20）另用琥珀色并显示具体 ATP 值；重复提醒还给了「合并重复项」按钮
      **待业务确认的取舍**：ATP 只与固定阈值（0/20）比，**从不与本行下单量比** —— ATP=500 时下单 10000 个不会有任何警告。是有意还是遗漏？
      走查中查出并修复：**事件种子按小数写 taxRate，与代码 SSOT（百分数）相反**。三处按 `taxRate/100` 算税的地方（metrics/order-items/order-pdf）对种子数据只算出 1/100 的税额，而 db:validate 不校验税额故一路绿灯。源头归一 + 同步下游 5 处计算（第一遍漏了 4 处，靠 grep 复查补齐），小数行 1444 → 0

- [ ] **Z11 生产库 99.4% 订单行税率为 NULL —— 是否系统性少计税**
      只读探测生产副本：NULL **1,330,196 行（99.4%）**、0 值 6,825、百分数 536、小数 11
      A2 只修了门户新单的写入路径，**存量完全没动，且这不是门户独有问题**
      发票接口 `app/api/invoices/route.ts:132` 对 NULL 取 0 → 历史发票可能都按 0 计税开出去了
      验收：查明历史发票的税额从哪来（是否另有来源）；若确为 0，评估影响面并给出回填方案
      ⚠️ **涉及已开出的发票与税务，必须由业务/财务确认后才动，不得自行回填**
      发现于：B1 周期

- [x] **D2 四类单据打印走查** —— 完成 20260811，commit `2693df1`
      产出：`docs/20260811-print-documents-walkthrough.md`
      验收达成：四类单据各打一份核对 ✓；**金额逐笔对上**（签收单 2308.20 / 2572.00 与库中 `totalAmount` 一致）✓；**司机名一致** ✓
      **台账点名的历史坑没有复发**：销售单「Driver: 2 pm SEAN」、三类配送单据「pm BAO」均与波次一致；全库校验行程与波次司机不一致数 **= 0**，`dispatch-loader` 的 `getOrderWaveDisplayMap` 修复仍在生效
      修复 P0：**三类配送单据全部打成空白** —— 种子写的 `Trip.restaurants` 字段名与应用主路径不一致（`customerId/customerName/orderId` vs `restaurantId/restaurantName/orderIds[]`），且不设 `waveId`；而 `trip-loader.ts` 读 `r.orderIds ?? []` → 空数组。**是测试数据缺陷不是应用缺陷**，但会让任何基于种子的打印验证得出「功能坏了」的错误结论
      修复 P1：发票**行级 VAT 显示 14%、汇总显示 13.50%**（行级 `toFixed(0)` 整数化，而 IE 的 13.5%/4.8% 都带小数）→ 新增 `formatVatRate()` + 3 个单测
      记录未改：打印页加载失败永远停在 Loading 无错误态；为打印一张单拉全部客户（1MB/1331 条）；发票 Total 与 Subtotal+VAT 差 1 分
      自动化经验：打印页内嵌 `window.print()` 会弹对话框阻塞 JS，`browser_evaluate`/`screenshot` 都会挂住，**须用 `browser_snapshot`**

- [x] **E3 库存守恒只读体检** —— 完成 20260811，commit `22ddce7`
      产出：`scripts/audit/stock-conservation-probe.ts`（只读，可复跑）+ `docs/20260811-stock-conservation-audit.md`
      验收达成：全量 5479 个商品逐个核对（超出「抽 20 个」的要求）✓；不守恒清单 + 量级分布 + 三分类已输出 ✓；未做任何清洗 ✓
      **⚠️ 推翻了本条原备注**：「已知存量 895 个商品不守恒」不成立，实测只有 **7 个**（A 类 1 / B 类 6 / C 类 0）。那个数字来自更早期判断，早已过时
      **但发现了更严重的问题**：守恒通过是因为 5479 个商品里 4195 个既无库存也无流水，两边都空自然相等 —— 检查本身没有约束力。真正的缺口是 **149,348 张已确认订单 / 1,337,568 行，出库流水只有 604 条（覆盖率 0.4%）**。出库机制在代码里是通的（527 条来源为 ORDER），但历史订单几乎都没产生流水
      连带影响：缺货率 / 库存预警 / 安全库存 / 盘点差异 全都建立在与流水脱钩的 `qtyOnHand` 上 → **E5、D6、Z5 动工前必须先定 `qtyOnHand` 的权威来源**（已加入待决策 9）
      数据源限制：跑的是 Neon 冻结快照（08-04），生产已迁 droplet，需复跑确认

- [x] **B2 定价与提成重算比对** —— 完成 20260811，commit `02431b2`
      产出：`scripts/audit/pricing-commission-probe.ts` + `docs/20260811-pricing-commission-audit.md`
      **定价侧健康**：133 万个订单行 `subtotal == unitPrice × orderedQty` **零例外**，税前口径 SSOT 成立
      **提成侧空转**：149,189 张已完成订单的提成金额全为 0，冻结记录也是 0。三个输入同时缺，其中第二条是**链路问题不是配置问题**：
        · 商品件提成价 5479/5479 ✅ 源头完整
        · **订单行提成价快照 171 / 1,337,568（0.01%）⛔ 件提成因此恒为 0**
        · 客户提成率 9/1605、客户固定费 0/1605
      抽 20 张单重算：0 差异 —— 但 20 张全是「库中 0、重算也 0」，是**假性通过**，不具验证能力。报告里已把覆盖率放在比对结果前面，避免被这个 ✓ 误导
      ⚠️ 「链路是否仍在漏」的受控实验**没做成**：在测试库用 API 下单被 403 挡住（Z7）。Z7 修好后第一件事补这个实验
      定价重算刻意不做：历史成交价要用当时的价格表才能重算，拿今天的价格表比对，差异不代表 bug

- [x] **D5 打印全场景测试用例** —— 完成 20260811，commit `4be3810`
      产出：`scripts/audit/print-matrix-test.ts`（`npm run test:print-matrix`）
      验收达成：可执行用例文件 ✓；覆盖 **4 类单据 × 7 种状态 × 4 类筛选 + 6 个边界 = 23 例** ✓
      **超出原验收**：原写「本批只写不跑」（那是浏览器不可用时定的）。**没跑过的「可执行用例」不算可执行**，故写完即验，当前 21 通过 / 0 失败 / 2 未获验证
      设计取舍：断言**数据层**而非像素 —— 像素比对脆弱且看不出错在哪，而 D2 查出的三个真问题（配送单据打空白 / 司机名不一致 / 税率量纲错）全能在数据层断言，失败时直接指向根因
      **自我纠错**：第一版跑出 23/23 全绿，但其中 3 例是「库里没这种状态所以跳过」被记成了 ✅ —— 与 B2 提成、E3 库存守恒栽过的假性通过同一个坑。已把 skip 与 pass 分开计并单独列出「未获验证」清单
      顺带补缺口：种子把所有 COMPLETED 单都开票推成 LOCKED，快照里 COMPLETED 一张不剩；按确定性哈希留 1/6 不开票（现实中就该有「已送达、账期未到」的单），COMPLETED 0 → 29 张
      遗留：`CONFIRMED` / `WAVE_ASSIGNED` 在种子里是过渡态、停留窗口太窄，仍无数据可测（已在报告中标注为未获验证，不当作通过）

- [x] **E5 收货→库存 测试用例** —— 完成 20260811，commit `44baf05`
      产出：`scripts/audit/receipt-stock-test.ts`（`npm run test:receipt-stock`）
      验收达成：覆盖 **正常 / 少收 / 超收 / 部分收（分两次）/ 损坏 / 良损混合** 6 场景 ✓；每例做收货前后快照对比，断言**库存增量 + 流水条数 + 类型** ✓；另加 2 项守恒断言
      **测试逼出一个生产代码的真 bug**：6 个业务场景全过，但守恒失败 —— qtyOnHand 868 vs Σ流水 913，差 45 恰为两次损坏品的 30+15
        · 根因：收货损坏品分支写了一笔**正数** SCRAP 流水而 `qtyOnHand` 不动，直接破坏头号不变量。对照 `/api/scrap` 主路径是 `decrement` + `qty: -qty`，**两条路径符号约定相反**
        · 为何一直没暴露：种子从不制造损坏收货，这条路径**从来没有测试数据走过** —— `db:validate` 会查这个不变量，但没数据触发
        · 修法：记两笔 `IN(+qty)` 紧跟 `SCRAP(-qty)`，净额 0；更贴近事实（货到了随即判损），损耗仪表盘照常统计
      修复后 8/8 通过；`db:validate` 11 项全过；`npm test` 409 项 407 通过

- [x] **E2 移仓 MVP 规划文档** —— 完成 20260811，commit `de6d683`
      产出：`docs/20260811-stock-transfer-mvp.md`（两版方案）
      验收达成：单据结构 ✓、状态流 ✓、是否审核 ✓、是否影响成本 ✓、**明确「这版不做什么」** ✓
      **规划的核心判断**：`Warehouse` 实体不存在；温区是挂在**商品**上的单一属性（`Product.currentZoneId`），不是库存的维度；库存是每商品一个总数。系统现在**根本无法表达「A 仓 30 件、B 仓 20 件」** —— 所以移仓不是「补个界面」，而是「要不要给库存加一个维度」
      方案甲（1 个物理仓）：`TRANSFER` 流水类型 + `StockTransfer` 单据 + 一个页面，每次写两笔净额 0 的流水，守恒天然成立。**约 1–2 天**。代价：记录型而非核算型
      方案乙（2 个仓以上）：新增 `Warehouse` + `StockBalance`（仓×区×商品），`qtyOnHand` 退化为派生值，**所有读写库存的路径都要改**。**2–4 周且高风险**
      **建议先做方案甲**：生产库 5479 个商品只有 1669 个（30.5%）定位了温区，近七成连在哪个区都不知道；且 E3 已查出出库流水只覆盖 0.4% 的已确认订单 —— 在库存流水本身不完整时再加维度，会把不准确放大成不可追溯
      ⛔ 三件事需客户确认（几个物理仓 / 要不要审核 / 是否影响成本），**确认前不进开发**

- [x] **G4 成本核算方案文档** —— 完成 20260811，commit `db2fc15`
      产出：`docs/20260811-costing-design.md`
      验收达成：选型建议 ✓、损耗如何入成本 ✓、报表清单 ✓、明确「这版不做什么」 ✓
      **结论与预期相反：算法本来就对，缺的是数据**
        · 入库**已实现移动加权平均**（`goods-receipts:165`）；出库**已实现 FIFO 消耗批次**（`consumeLotsFIFO`）；毛利分析优先批次加权成本、回落标准成本，并返回 `costedAmount` 让前端显示覆盖率 —— 这个设计是清醒的
        · 数据侧三个硬缺口（生产副本实测）：**批次仅 45 个**、**SCRAP 仅 6 条**、**凭证 0 张**；结合 E3 的「出库流水覆盖 0.4%」，实际是算法对但几乎没有数据流经它
      建议：**不改算法**，优先补数据 —— 补出库流水 → 收货必建批次 → 损耗入成本 → 最后才是成本进账
      损耗口径：按环节分别归属（收货判损挂供应商索赔；分拣/仓储/运输计入营业成本并归因），**明确不摊回 `standardPrice`**（否则「成本」同时承载采购价与损耗率，毛利异常无法归因）
      **两处自我更正留痕**：原以为「毛利页没展示成本覆盖率」「损耗看板只有数量没金额」，实查发现**两者都已完备**；据此把报表 MVP 从三张收窄到一张（库存成本表，这个确实没有）

- [x] **H1 数据中心覆盖矩阵** —— 完成 20260811，commit `2dc582a`
      产出：`docs/20260811-analytics-coverage.md`（实查全部 **17 个分析页 + 16 个接口**后成文）
      验收达成：指标 × 现有页面覆盖矩阵 ✓；缺失项 MVP 定义 ✓；明确「这版不做什么」 ✓
      **需求「四」点名的四个指标全部已实现**：日销售额 / 客单价 / 缺货率 / 关键商品销量 —— 且是 `sales-overview` 一个接口一次返回，缺货率与 `/analytics/shortage` **共用同一份计算**，不会两处跑偏
      **需求「八」点名的三块**：经营看板 ✅ 已有（首页渲染销售额/毛利/应收/缺货/波次/趋势/库存）、客户分析 ✅ 已有且含 ABC 分层与流失预警、**商品分析 ⛔ 唯一真缺口**
      商品维度散在 `topProducts`（只有前 10 名）/ `shortage` / `procurement` 三处，**没有以商品为主体的入口**。MVP：搜一个商品，一屏看销量趋势、毛利、缺货次数、损耗、采购价走势 —— 数据来源全部已存在，**1 页面 + 1 聚合接口，约 2–3 天**
      明确不做：不新建经营看板/客户分析、不重做四个日销售指标（会制造第二套口径）、不做整合大屏

- [x] **I2 权限调整操作指南** —— 完成 20260811，commit `f980baa`
      产出：帮助页新增「🔐 角色与权限管理」章节（4 篇）+ `scripts/rbac/detect-inert-permissions.ts`
      验收达成：**进系统内帮助页** ✓（浏览器实测可读）；**非技术人员可独立照做** ✓ —— 指南照着**真实界面**写（先把用户页/角色页/新建角色弹窗都点过一遍），不是凭想象
      四篇：三个概念（权限点/角色/数据范围，说明权限取并集、范围取最宽）· 新建角色 7 步（并给出「复制现有角色再改」的省事路子）· 分配给用户（含「变更后必须重登，改完通知本人」）· **改完一定要验证**（只看配置页勾了什么是验不出问题的）
      **备注里的「8 个假开关」已过期 —— 实测 13 个**：`sales.order.confirm`、`sales.order.cancel`、`dispatch.console.access`、`finance.invoice.cancel` 等。已固化检测脚本，文件头写明「数字随代码演进而变，不要引用历史结论，要用当场跑」
      ⚠️ 假开关**未清理**（清理需逐个决定「补判定」还是「删权限点」，属开发决策）；已在指南最后一篇如实写明，提醒涉及这几个动作的限制不要只靠勾选

> ~~C1 司机端 APP 形态选型~~ —— **决策已定 20260811：APP 本期不做**，先把 Web 端 C4~C10 补完测通。C1/C2/C3 移入 W4 挂起。

---

## W2 · 需要写库（Z1 关闭后开跑）

- [x] **A4 Order 增加来源字段 + 列表可筛选** —— 完成 20260811，commit `c7ffb9d`
      产出：`enum OrderSource`（PORTAL/INTERNAL/IMPORT）+ `Order.source`（默认 INTERNAL）+ 手写迁移（含历史回填）+ 门户写入点 + 列表接口 `?source=` + 报价单列表「Source」列与筛选下拉
      验收达成：门户 API 下单 → `RE-260811-003.source = PORTAL` ✓；历史 217 单全部有值无 NULL ✓；筛选 PORTAL=2 / INTERNAL=16 / 不筛=18 / 多选=2 ✓；非法值 `source=BOGUS` 返回 200 且等同不筛（非 500）✓；typecheck + 399 单测 + `db:validate` 11 项全绿 ✓
      设计取舍：历史回填**只认审计日志里明确记了 `customer-portal` 的单**，不按「创建者是不是餐厅账号」之类启发式猜——猜错会让「这单是客户自己下的」失去可信度，而那正是本字段的全部价值
- [x] **A3 门户下单 → Quotation 列表页** —— 完成 20260811，commit `f9899e8`
      产出：`scripts/audit/verify-portal-to-quotation.ts`（可复跑的端到端脚本，走真实 HTTP 接口）
      验收达成：**10/10 断言通过**（新单 `RE-260811-004`，金额 418.55 与行小计一致）
        · 建单成功且状态 PENDING —— 本系统里 quotation 就是 `status=PENDING` 的 Order ✓
        · 出现在 quotation 列表默认视图 ✓
        · `source=PORTAL` 可识别，按来源筛选能筛到且不混入其他来源 ✓
        · 行数/每行数量与提交一致；`totalAmount == Σ行小计`；每行 `subtotal == unitPrice × qty`；单价由服务端权威定价填充 ✓
      刻意走 HTTP 而非直接调 Prisma：直接写库只能证明「数据能长成这样」，证明不了「用户那样操作时系统真的这么做」——B2 已经证实这个区别有多要命（提成代码全实现、单测全过，对 15 万单产出 0）
      ⚠️ **页面视觉未验**：列表页的 Source 列在浏览器里长什么样、筛选下拉点起来如何，仍被 Z9 挡着。接口层已完全验证
- [x] **I3 商品大小单位全链路实测** — 8/8 通过 `7720876`
      查出 2 个真 bug：①收货从不调 `toStockQty`，收 5 箱只加 5 件（销售侧一直有换算，两边不对称）
      ②加权平均成本未随数量换算会虚高 12 倍；另补 `OrderLine.uomName` 服务端快照（此前全听客户端，
      不传即 NULL，拣货单上只有数字）。
      ⚠️ **数据事实：测试库 1677 个商品模板 `uomId` 全部为 NULL** —— 没有任何商品设了基准单位，
      大小单位机制没有生效前提。生产库需同样核对（若同样为空，本次修复对现网无立即效果，
      但一旦开始设单位就会直接生效）。回归脚本 `scripts/audit/uom-chain-test.ts`。
- [x] **D4 拣货单按整箱/零散拆分** — 11/11 通过 `fb884de` + `468a8b3`
      `lib/pack-split.ts` 纯函数（11 单测）+ 接进拣货单模板：35 包 → 印「35」和「2 CASE + 11 PCS」。
      按大单位下单时不重复拆（否则印成「0 箱 + 2 箱」）。
      顺带补分页控制：此前 `@media print` **只有页边距**，多页拣货单会行被截断、第二页没表头。
      `picking-pdf` 加 `?format=html` 预览口（同鉴权同权限点），调样式与自动化断言都靠它。
      ⚠️ **与已有 `PickingVariant`（纸面「整箱整袋 STOCKABLE」/「零散货 CONSUMABLE」）不是一回事** ——
      那个按 `ProductTemplate.type` 分表，是「哪类货」；本次按数量拆，是「这个数怎么拿」。措辞撞车，改动别混。
      ⚠️ **数据事实：`ProductSaleUom` 0 行**，没有任何商品配了可售单位 → 箱规无从推出，拆箱对现有数据
      一律返回 null，纸面与改动前完全一致。与 I3「1677 个模板 uomId 全空」是同一个坑的两面。
- [x] **D1 打单工作台** — 10/10 通过 `db11ff3`
      三项验收对现状：**待打印列表已有**（打印中心本就是），只补「还剩 N 批未打」；
      **留痕地基好但接错地方** —— 「✓ 已打印」存在 localStorage，换电脑就没了，还有个「清除已打印记录」
      按钮能抹掉；而服务端每次打印本就写了 ActionLog（生产副本 358+ 条）。新增 `/api/waves/print-status`
      把这份已有数据聚合出来，**不新增写入路径**；**重打确认此前完全没有**且是刻意静默的，改为打过才问。
      唯一来源定为 pick-lock（每条打印路径必经），print-log 打 `printLogOnly` 记号避免一次算两次。
      ⚠️ 安全绳测出**旧 token 可达性缺口**：FINANCE/SALES 能打印却够不到 print-status，
      部署后未重登的人会 403 → 前端退回"全都没打过" → 全量重打。已补旧路径白名单。
      ⚠️ 顺带查明**之前几个周期「页面视觉未验」的真正原因**：standalone 产物要手工拷
      `.next/static`+`public`，漏了整页 ChunkLoadError。已固化 `npm run build:standalone`。
- [x] **D3 打印按客户/线路/商品筛选** — 23/23 通过 `8c2b3c0`
      三维正交：**线路**=挑哪几个波次（本来就有），**客户**=订单级，**商品**=行级（行被留空的订单整单不打）。
      `lib/print/print-filters.ts` 纯函数层**被服务端加载器与前端预览共用** —— 「筛选结果条数与预览一致」
      这条验收靠共用实现保证，不是两边各写一遍再比对（司机 slot 分叉、税率量纲都是那么栽的）。
      三处一起改，漏一处就「屏幕上筛了、打出来是全量」且不报错：① 参数解析原先在三个路由各抄一遍，
      收口成 `parseDispatchSelector()`；② 商品筛选砍行后必须重算 `totalAmount`（汇总单读的就是它，
      不重算会印出「只列 2 行、金额却是全单」）；③ 筛选要在截断之前做，否则是先截 200 张再筛。
      纸面提示 `renderTripNoticeHtml()` 收口四种单据 —— 原先只有送货单/销售单渲染 `trip.notice`，
      **拣货单与汇总单静默忽略**，而「只打了一部分」恰恰是拣货单最该说清楚的。
      验证设计：11 个组合每例同时比「预览 == 打印」和「== 预期」，预览侧按打印中心的取数方式重来一遍
      （两条路径数据来源不同才有意义）；PDF 路由用「必然空集的筛选须 404」证明参数真作用到了 ——
      正例只能证明「能出 PDF」，证明不了筛选没被忽略。浏览器实点确认请求带上了 `customerIds`。
      ⚠️ **顺带修 `POST /api/orders` 的客户名**：原先只认客户端传的 `restaurantName`，不传就写死
      「未知餐馆」。界面永远传所以没人发现；脚本/集成建的单客户名是废话，客户筛选会列出一串同名项。
      改为回落到客户档案。本地库存量 19 张（全来自历次审计脚本），门户单 0 张 —— 现网无影响。
- [x] **D6 缺货自动筛选 + 批量改单/转单 + 原因** — 25/25 通过 `4191120`
      **先把台账点名要定的那件事定了 —— 与拣货锁的优先级**：锁定批次下 **减量/删行放行、加量拦截**。
      拣货锁是打印拣货单时自动上的，而缺货恰恰是拣货中发现的，彻底锁死等于让缺货 tab 没法用；
      加量必须先解锁，否则等于借缺货接口在锁定期偷偷加单。⚠️ 记忆/台账里「用户选彻底锁、推翻 PRD 缺货豁免」
      **已过期**：20260708 确实那么改过，20260718 的提交又按 PRD Feature D 放开了减量，两个口径并存至今没人明说。
      本周期写进路由文件头 + 测试钉死（同时测「该放的放了」和「该拦的拦了」，只测一边都是自欺）。
      产出：`POST /api/daily-sales/shortage/bulk-adjust`（ADJUST 批量改量 / DEFER 转次日）+ 勾选与批量填充 UI
      + `lib/shortage-reason.ts`（原因白名单，必填、不给默认值）。转次日为该客户建 **PENDING 草稿单**
      （缺的货此刻并不存在，确认才扣库存），同客户同日合并成一张，单价照抄原单快照（缺货是我们的错，
      不该顺带按次日价格重算）。原因刻意不新建表，跟 ActionLog + OrderAuditLog 两条已有轨迹走。
      ⚠️ **顺带查出 E3「7 个商品不守恒」的成因**：`PATCH/DELETE /api/orders/[id]/lines/[lineId]`
      （缺货改量走的就是它）改完 `qtyOnHand` **不写 StockMove**、也不做大小单位换算，
      而同一件事 `PUT /api/orders/[id]` 三件都做了 —— 两条路径记账方式相反且都不报错。
      生产副本实测：81 条缺货改量日志 / 27 单 / 134 商品，其中 **6 个不守恒**（E3 全库查出 7 个），
      差额与改量幅度对得上（如 Beansprout BAG 107 vs 81）。已收成 `lib/order-line-stock.ts`。
      ⚠️ `PUT /api/orders/[id]` 里那段等价逻辑**未改写**（纠缠在整单 diff 里，风险高），
      两处的记账约定必须保持一致，改任一处都要同时看另一处。
- [x] **D8 实时统计：日销售额/关键商品销量/客单价/缺货率** — 13/13 通过 `10da755`
      四个指标 H1 已核实全部实现，所以本条做的是验收标准要求的那件事：**口径收进 `lib/analytics/metrics.ts`**。
      收的过程中查出**三处真错位**：
      · **快照按进程本地时区切日**（`setHours(0,0,0,0)`），而它的 key 与查询范围按都柏林切 —— 生产容器是 UTC，
        夏令时期间都柏林 00:00–01:00 的单被算进前一天。生产副本当场复测 **1,909 / 149,859 单（1.27%）** 属这批。
        新增 `businessDayStart/Range/TodayStart/addBusinessDays`，snapshot 与 sales-overview 全改用它们。
        ⚠️ **存量快照行仍是旧口径**，要 `POST /api/analytics/snapshots` 重算才会变，否则只有新的一天是对的。
      · **缺货率整条按天序列偏一天**：days CTE 的 `generate_series($1::date …)` 里，$1 是「都柏林某日 00:00」
        对应的 UTC 时刻，夏令时期间长得像前一天 23:00，`::date` 就取成前一天 → 查「今天」返回的是**昨天**那行，
        订单行恒 0、缺货率恒 0。改为在 JS 里按业务日展开日期列表再 unnest 传入。
      · **客单价公式散在三处**（服务端 + SalesStats + boss 页各写一遍）→ 收成 `summarizeSalesSeries()`，
        接口返回 summary，两页都读它。区间客单价必须是 Σ销售额/Σ订单数，不是「每天客单价再平均」（实测 46 vs 15.33）。
      ⚠️ **顺带补上 `db push` 跳过的非表对象**（Z6 延伸，D8 撞出来的）：视图与 pg_trgm 写在迁移 SQL 里，
      全新库因此缺 `v_lot_daily_cost` → **分析中心直接 500**、缺 `veggie_sales_report` → 报表查询报错、
      缺 trgm 索引 → 分面搜索全表扫描。新增 `scripts/db/apply-sql-objects.ts` 并接进 `db:bootstrap` 第 2 步。
      它只执行每个对象的**最新**定义而非重放历史链 —— 20260522 那版视图里还有早已删掉的 `o.salesman`，
      重放旧版必报 column does not exist。**重建要的是终态，不是历史。**
- [x] **D9 按日/按周销售+库存+商品数量视图** — 18 单测 + 浏览器实测 `afd77d7`
      **先核实现状再动手**：星期汇总表已有、库存/ATP 在「按分类」视图已有、CSV 导出有但只导
      「订单汇总/产品明细」——**矩阵本身导不出来**，而那正是采购要的那张纸。所以本次只做三件：
      ① 同一张表加「按日 / 按周」切换（按周=周一至周日七列、跨周同星期几相加；按日=每个配送日一列）；
      ② 矩阵直接带**当前库存**与**可承诺 ATP**（与「按分类」同一个式子），采购要的
      「每天走多少 / 还剩多少 / 够不够」一张表看全；③ 导出矩阵 CSV，**用的就是屏幕上那份结果对象**，
      不重新聚合 —— 「导出的和屏幕不一样」从结构上不可能发生。
      聚合收进 `lib/analytics/sales-matrix.ts`（纯函数）。
      ⚠️ **口径变更**：矩阵按 `productId` 归并，不再按商品名 —— 按名字会把同名不同规格并成一行
      （A1 走查记过），而库存/ATP 是按商品 id 的，并行后那两列没法解释。合计不变，只是行拆得更细。
      交叉印证：按日 08-04/05/06/12 = 145/94/87/20 合计 346；切按周 周二 145/周三 114/周四 87 = 346，
      **周三 114 恰是 94+20**，两种切法互证；金额 €18177.90 与页面「筛选结果」合计一致。
      导出实点：文件名带区间，头三字节 `EF BB BF`（BOM，Excel 中文不乱码），合计行与屏幕逐格相同。
- [x] **E4 损耗录入界面 + 归因** — 18/18 通过 `5502421`
      **先核实现状，两处与清单记载不符**：清单写「录入界面是缺的」——界面**在**（`inventory/scrap`，390 行带弹窗），
      但它**根本用不了**：弹窗按 `p.template?.type === 'PRODUCT'` 筛商品，而 `GET /api/products` 的响应
      把 template 解构掉了、从不返回 type → 判断恒为 undefined → **选品列表永远是空的**，报废录不进去。
      同一写法在批次页也有一份。改为服务端筛（新增 `?templateType=PRODUCT`）—— 让客户端过滤一个
      接口不返回的字段，错了不报错，只会安静地筛出空集。
      **真正缺的是「环节」**：原有 6 个原因把环节与原因混在一起（WAREHOUSE_* / RECEIPT_DAMAGE），
      而需求要按分拣/运输/仓储归因。两者正交 —— 同样是「损坏」，收货时发现是供应商责任可索赔，
      分拣时发生是自家操作。产出：`StockMove.lossStage/lossReason` 两列（手写迁移，**历史行留 NULL 不回填**）、
      录入弹窗「损耗环节」下拉 + 责任提示、看板「损耗环节拆分」卡片、客退报废自动带 CUSTOMER_RETURN。
      原因排行改读结构化字段，note 正则降级为**兜底**（改一句文案就静默把统计打散，正是这次要终结的）。
      顺带把括号内文案登记为原因别名：`parseReasonLabel` 优先取括号内文字，
      「到货即损坏（运输/供应商责任）」被截断后查不中 key，本地 **45 件**因此躺在「未归因」，修好后归入「收货」并标注推断。
      浏览器实点：选品可搜可选（修复前搜不到）、提交后列表出现「运输环节 - 仓库损坏 - …」、
      看板显示 收货 45（其中 45 推断）/ 分拣 24 / 运输 14 / 仓储 3。
- [x] **E5x 收货→库存 用例执行** — 17/17 通过 `126b71c`
      **如实报结果：原有 6 场景 8/8 全过，没有失败项要修。** 但「跑一遍全绿」不等于这条链验过了 ——
      那 6 例只断言 `qtyOnHand` 与 `StockMove`，而收货同时还是**批次与成本的唯一入口**，那一腿没人守。
      G4 查出的「批次仅 45 个 / 凭证 0 张」结论正是「算法对但几乎没有数据流经它」。故把用例扩到 17 例：
      收货建 Lot ✓、批次余量 = 收货量 ✓、批次成本 = 采购单价 ✓、
      移动加权平均 (100×10+50×16)/150 = 12 ✓（用**专用商品**算，借共享商品会被别的用例改掉期望值）、
      I3 回归「按箱收货 5 箱 = 60 件且单价折算 €240/箱 → €20/件」✓、损坏品不建批次 ✓、全库批次守恒 ✓。
      顺带补 E4 上一周期留下的缺口：收货损坏那笔 SCRAP 没带 `lossStage/lossReason`，
      看板只能按 note 反推 —— 而「到货即损坏」正是能否向供应商索赔的那一格，现已显式带 RECEIPT。
      ⚠️ **一条口径未收口**：「超收（订 50 收 80）」用例目前接受两种行为（拒绝 / 按实收入库），
      只排除了「按订购量入 50」。该不该拦超收是业务决策 → 见待决策 11。
- [x] **E6 收货与采购单自动关联 + 预计到货** — 12/12 通过 `83c5cd8`
      **① 一键生成 + 行项带入：已存在**（PO 详情「确认收货」深链 `?poId=`，按「订购 − 已收」预填），本轮只补测试守住。
      **② 收货单显示预计到货日：缺** —— 待收货列表里有，进了收货表单反而没有。现在表单头部与收货历史都显示
      预计到货日 + 偏差徽标（迟到红/提前蓝/按期绿）。
      **③ 未关联采购单的收货：缺，且容易做成假的** —— `GoodsReceipt.purchaseOrderId` 是**非空外键**，
      去那张表查「未关联」永远 0 条，看着像「全都关联好了」。真正会漏的是**绕过收货单直接进库存的流水**
      （手工调整/导入/盘盈）。新增 `GET /api/goods-receipts/unlinked` + 收货记录页提示区块，
      判据用**白名单反选**：将来新增的入库路径若忘登记会自动落进「未关联」被看见，而不是静默漏掉。
      ⚠️ **实测中当场修正两处**：
      · **计数被上限截断** —— 第一版先取 1000 条再内存筛，count 停在 limit 上（测试「未关联 +1」恒不成立：200→200）。
        改为条件下推到 DB 做 count/aggregate。否则界面显示「200 笔未关联」而真实可能几千笔。
      · **期初余额把信号淹了** —— 30 天内 **1,583/1,650** 笔入库都是 Z5 期初余额（`sourceRef=OPENING-BALANCE`）。
        按 sourceRef 精确排除后未关联从 1,611 降到 **29 笔**。只排期初、**不放行整个 ADJUSTMENT 类型**：
        正数调整恰恰是「悄悄把货塞进库存」最可能的路径。
- [x] **E7 实际到货日期回写采购单** — 17/17 通过 `ca7ec3f`
      「分批收货按最后一次还是首次（需确认）」**没有卡住等确认**：两个都存
      （`firstArrivedAt` 首次到货 / `lastArrivedAt` 收齐日），它们回答不同问题且都免费维护，
      客户日后改口径不用回头补历史。
      **准时率口径写死在 `lib/receipt-linkage.summarizeOnTime` 并刻意从严**：按收齐日 vs 预计到货日；
      **只统计已收齐的单**（未收齐进 pending 不进分母 —— 否则永远收不齐的单会被静默算成「按期」）；
      提前到货算准时；没填预计到货日的单独计数、绝不拿下单日顶替；一单可判定都没有时
      **rate 返回 null 而不是 0**（0% 会被读成「这家从不准时」）。
      ⚠️ 写回有个易错点：**不能「first 为空才填、last 直接覆盖」** —— 收货单允许补录，
      补一张更早的进来时 first 要前移、last 不能被旧的顶掉。改为按 GoodsReceipt 的 MIN/MAX 取，
      与录入顺序无关；测试专门造了这个场景（补录 5 天前 → first 前移、last 不变）。
      迁移带**存量回填**（由收货单 MIN/MAX 派生，确定且幂等，无收货单的保持 NULL），本地实测 1 → 43 张。
      展示落在已有的采购分析「供应商 & 满足率」表，新增「到货准时率」列（85%/26 = 百分比 + 已判定单数）。
- [x] **D5x 打印用例执行** — 30/30 通过 `7d81ffc`
      **如实报结果：原 23 例 0 失败**，且 D5 记为「未获验证」的 CONFIRMED / WAVE_ASSIGNED 两态
      因后续周期的夹具有了数据，本轮真正验过了。
      **但「全绿」恰恰是问题**：筛选那组的断言是 `Array.isArray(d.orders)` ——「接口没崩就算过」，
      **什么都没筛掉的筛选照样 ✅**。实测「按线路」返回的就是整日全量 8 单（取样到的波次
      `driverSlotId` 为空，传空参数退化成整日全打），而断言看不出来。与 B2 提成、E3 守恒同类，
      只是藏在一个看起来很朴素的 isArray 里。
      修法：每一维都断言「确实筛窄了」——按波次 ≤ 波次订单数；**按线路改为专挑真有 driverSlotId 的波次**
      （全库 129 个非空波次里 125 个有 slot，最新那个恰好没有，按「最新」取样正好漏测）；
      按客户断言每单都属该客户；按商品断言每单剩余行**全部**是该商品；组合筛选必须 ≤ 单维。
      并补上矩阵自称覆盖、实际没测的两块：筛选维度 4 → 6 类（新增客户/商品，即 D3 那两维）；
      **4 类单据 × 筛选** —— 拿筛后数据把四种模板都渲染一遍，断言都有表格且都带「非该批次全部内容」提示
      （拣货单与汇总单的提示是 D3 才补的，少一张纸没提示，仓库就会把部分拣货单当成整车全部）。
- [x] **F1 采购计划与预测 MVP** — 16/16 通过 `138ba3c`
      界面与算法此前都已存在（建议页 868 行、公式在 `lib/purchase-suggestions-fresh.ts`），本轮是**把链真跑一遍**，
      查出三个会让采购照着错数字下单的问题：
      · **全新库里这块功能是死的**：生成器第一行查 `CategoryGroup(FRESH_FROZEN)`，而该表**只由一次性订正脚本**
        `scripts/backfill-category-groups.ts` 创建 —— 不在 seed、不在迁移数据里，测试库实测为空 → 生成器返回 `[]`。
        **第三次撞上同一类问题**（Z7 的 RBAC 数据迁移、D8 的视图），已接进 `db:bootstrap` 第 5 步。
      · **转成采购单后第二天还会把同一批货再建议一遍**：在途量只算 `status='CONFIRMED'`，
        而「建议转采购单」生成的是 **DRAFT** 单 → 采纳 12 件后建议依然是 20 件，照着下单就是重复采购。
        改为 DRAFT/SENT/TO_APPROVE/CONFIRMED 都算在途（RECEIVED 及之后不算，货已进 qtyOnHand）。
        复测：转单 12 → 新建议 20 → 8，正好差 12。
      · **采纳后建议行自相矛盾**：只改 `suggestedQty` 没改 `estimatedCost`，列表显示「建议采购 12 · 预估成本 €100」
        （€100 按原建议 20 算的）—— 浏览器走查肉眼发现，现两个一起改。
      顺带把「近 3 日出货」的日界从进程本地时区改到业务时区（D8 建立的 `businessTodayStart`）。
      验证要点：**四个输入全部造成已知值**（出货 30/3日、未来 25、库存 10、在途 5），断言建议量恰好 = 20 ——
      只看「有没有生成建议」证明不了它算得对，而建议量算错比没有建议更糟。
- [x] **F2 询价单 PDF 纯解析** — 9/9 通过 `fadd266`
      现有实现是把整份文字丢给 Claude 出 JSON，而需求写的是「**暂不上 AI**，做最简方案」——
      没配 Key 的环境（私有化部署正是）直接不可用，且同一份 PDF 每次结果可能不同。
      新增 `lib/purchase/pdf-line-parser.ts`（纯函数、不联网）：表头驱动 + 模式兜底；
      路由改为**解析器优先、AI 只兜底**，响应标出 `source=parser|ai|none`。
      **三件事是实测撞出来的**：
      · **standalone 上这功能根本跑不起来** —— pdf-parse 被打进 chunk 后 import 的
        `./pdf.worker.mjs` 指向不存在的路径，一上传就 500；dev 不打包所以看不见。
        改为外部化 pdf-parse + 把 pdfjs worker 加进 `outputFileTracingIncludes`（与既有 @napi-rs 同病根）。
      · **真实 PDF 一行商品会折成好几行** —— 客户单据的文字层是
        `1.000 LOOSE Courgette LOOSE` / `角瓜` / `1.20 0% € 1.20` 三行，只按物理行读单价永远 null。
      · **表头两词挤一格** —— `QTY UNIT | … | UNIT COST`，全等匹配一个都认不出 → 退化兜底 →
        把行小计 €75 当单价（真实 €5）。改「包含」匹配且 price 先于 uom 认领；兜底另加验算
        （数量 × 倒数第二 ≈ 最后一个 ⇒ 最后一个是小计）。
      失败路径按验收要求**不返回空表**：报错写明扫了多少行、跳过多少合计行，
      让人分得清「这份 PDF 没有商品」和「我们没解析出来」。
      ⚠️ 顺带修好**守卫自身的假阳性**：`tests/no-direct-cloud-sdk` 用 `includes()` 判定，
      一句「本文件不直连 @google-cloud/storage」的注释就把它弄红；改为只认 import/require/动态 import
      三种语法，并加自测。**第 N 次「度量工具自身失真」。**
      ⚠️ **真实供应商询价单 PDF 客户尚未提供** → 见待决策 12。本轮用「客户自家单据 + 系统生成单据」替代，
      不阻塞其余部分；拿到文件后放进 `test/` 并在脚本里加一例即可复跑。
- [x] **F3 采购单全生命周期实测** — 21/21 通过 `cdfe6ab` + `5d0b94a`
      创建 → 草稿改 → 审核 → 收货 → 退货 → 账单过账 → INVOICED → LOCKED 全程实打并核对落库。
      **② 审核权限用真的没有审批权的司机账号去打**（403，且被拒后状态没被改动 ——
      「返回 403 但事已经做了」是最难查的一类漏洞）。只看代码写了 requirePermission 不算数：本项目出过 13 个装饰性权限点。
      **③ 退货这条路原本不存在** —— 收货只进不出，货收错只能记「报废」，可那是自家损失，
      与「退回供应商、应付跟着减」是两回事。新增 `POST /api/purchase-orders/:id/return`：
      退货量 ≤ 已收量（超了 409）、扣库存 + FIFO 回退批次、回冲 `receivedQty`（不冲这单看起来还是收齐的，
      采购建议还会把退掉的货当在途）、**刻意不动账单金额**（账单是财务凭证，抵扣该由财务开贷记单）。
      **④ 对账**：账单税前 €180 = 实收 €156 + 应退 €24。
      ⚠️ **新路由必须在 route-map 显式登记** —— 未登记的路由在 gate 里是**默认拒绝**，表现为整个功能 403（本条就是这么发现的）。
      ⚠️ **提交后 db:validate 报出批次守恒 1/162 违例，是我自己写的退货路由造成的**：
      只扣了 `Lot.currentQty` 却把流水记成 `lotId=null`，**商品级守恒照样成立、只有批次级露馅** ——
      「只测总量」漏掉的正是这一类。已改为按批次逐笔记流水；**同一隐患在 `/api/scrap` 也存在**
      （报废测试用的商品都没批次所以从没暴露），一并修掉，并在用例里补了批次级断言。
- [x] **F4 采购质检 MVP（并入收货界面）** — 26/26 通过 + 12 单测 + 浏览器实点 `5c0773a`
      收货表格新增「质检」列，逐行展开填**实测重量 / 新鲜度评级 / 农残检测**，三项全可留空；
      拒收数量与原因同在该面板（拒收必须给原因才能提交）。默认收起，不做质检的日常收货一步没多。
      **⛔ 刻意不新建 QualityCheck 表**：质检是「在收货那一刻对某一行货」的判断，宿主就是收货行。
      另起一张表马上会有第二处真相（收货行说收了 60、质检表说判了 100，谁对？）。
      采购单详情与批次追溯都是**读取派生** —— 后者按 `Lot.sourceId` 反查收货单，抄一份到 Lot 上
      就会出现「收货单改了、批次上的没改」。
      **⛔ 结论（合格/不合格）也不让人另填**，由 `qcVerdict()` 从三项体检值派生：
      手选结论必然出现「农残 FAIL 但勾了 PASS」，而这是食品安全上最不能含糊的一格。
      **拒收 ≠ 报废，这是本条的实质**：`condition='rejected'` 不入库、不建批次、**不写任何流水**、
      **不计入 receivedQty** —— 计入了这张 PO 就显示「已收齐」，而那批货正躺在供应商车上，再没人追。
      浏览器实测印证：EVT-PO-00012 收 48 拒 6 后，待收货列表由「3 个待收货」变「1 个待收货」。
      拒收另记一条挂在**采购单**上的 ActionLog（收货单的日志采购员不会去翻），PO chatter 当场可见。
      两条校验规则（`lib/purchase/qc.ts`，前后端共用同一份纯函数）：
      ① 拒收必须选白名单原因；② **农残超标却一件都没拒收，必须写明让步接收理由** ——
      不是拦截（现实中会先收下再复检），是逼这个决定留下署名和说法。
      写端严格读端宽松：非法枚举写入时 400（静默丢弃 = 界面填了库里没有，而操作员看到提交成功），
      读取时忽略（一条脏记录不该把追溯页打成 500）。签字人服务端盖章，实测客户端传的假名字被忽略。
      ⚠️ **已知精度**：批次→质检按 `productId` 匹配收货行。同一张收货单里同一商品拆成多条良品行时
      （界面不会这么产出，脚本可能）只能定位到第一条。质检记录也只挂在良品/损坏/拒收三条中的第一条上——
      抄三份会让「质检了几行」翻三倍。
      ⚠️ **本条没做**：重量偏差不做自动判定（`ProductSaleUom` 0 行、模板 `uomId` 全空，
      推不出「应该多重」，见 I3/D4 同一个坑）；不做质检合格率报表（属 H2 透视范畴）。
- [x] **G1 客户结算 MVP + 司机现金核销** — 28/28 通过 + 11 单测 + 浏览器实测 `cc6da33`
      **先核实现状，与清单记载有出入**：需求写「目前没有界面」——对账单列表页与详情页**都在**（398 行），
      司机现金核销的落库逻辑（`lib/trip-settlement-payment.postTripCollections`）也在，且设计清醒
      （不自动过账 DRAFT 发票、按到期日先后核销、靠 note 里的 `TRIP:` 标记幂等）。
      真缺口是**生成算法有四处错位**，加上**明细逐笔对不上**（页面只有四个汇总数字，没有构成它们的单据）。
      四处错位（`lib/finance/statement.ts` 收口，各有单测钉住）：
      · **末日整天被切掉** —— `lte: new Date('2026-08-31')` 的上界是 UTC 00:00 = 都柏林 01:00，
        末日 23 小时的单一张都不算。月末对账少一天且金额"看着差不多"，最难发现。改左闭右开到次日 00:00。
      · **日界按 UTC 而非业务时区** —— 与 D8 同一个坑。
      · **销售口径用 createdAt** —— 全系统销售口径 SSOT 是 `Order.confirmationDate`；两套日期意味着
        对账单的「本期销售额」与数据中心的销售额对不上，客户拿两张纸来问时没人解释得清。
      · **首张对账单期初恒为 0** —— 原实现只认「上一张对账单的期末」，没有上一张就当 0。
        而**这正是本任务的场景**（生产库一张都没有）：客户明明欠着钱，第一张单却从 0 起算。
        改为无上期时从历史派生（期前销售 − 期前收款），并在响应里标 `openingSource`。
      新增 `GET /api/statements/:id?withDetail=1`：订单逐笔（按**存下来的 orderIds**取，
      不按期间重查——否则事后新确认的单会凭空出现在一张已发出的账上）+ 收款逐笔（标注司机现金/汇款、
      指到具体发票与行程）+ **服务端当场算的核对结果**。「逐笔对上」不靠人肉比，人肉比查不出 3 分钱差。
      核对差额三项分开报（销售差 / 收款差 / 恒等式差）——混成一个 ✗ 就没法排查。
      并加**重复生成 409**：同客户同期间两张并存时，下一期取「上一张」会变成掷骰子，客户还会收到两份数字不同的账。
      ⚠️ **浏览器实测当场看见一个真瑕疵并修掉**：`Payment.createdBy` 两条写入路径语义相反 ——
      手工登记写人名、交账核销写 userId，于是明细「经手」列一行显示「财务小陈」、一行显示一串 cuid。
      已统一为人名，并在脚本里断言「不能长得像 cuid」。
      ⚠️ **职责分离是对的，是我第一版脚本用错了身份**：交账**司机提交**（driver 有 `finance.settlement.create`）、
      **财务确认**（`finance.settlement.confirm`），财务账号提交会 403、司机确认也会 403。两条都已入例。
      ⚠️ **本条没做**：不做自动按周期批量出账（需先定客户账期日历，属 G3/客户决策）；
      不改发票过账流程（过账是财务动作，不该是交账的副作用——沿用既有设计）。
      ⚠️ **验收里「生产库里真的出现第一张 Statement」在本地测试库达成**（Statement 70 → 72、Payment 101 → 102，
      全程走真实 HTTP）。**生产库写入不在本轮授权范围**（硬停止条件：任何需要写生产库的动作 → 停），
      需在客户环境跑一次同样的流程才算最终闭环。
- [x] **G2 供应商结算 + 分批付款** — 20/20 通过 + 7 单测 + 浏览器实测 `7dfed4d`
      **先核实现状**：「按采购单自动生成供应商对账单」**已存在**（PO 确认即生成 DRAFT 账单，本轮实测 VB-00014 €300 ✓）；
      账龄接口也在且口径清醒（只统计 POSTED 且 amountDue>0，无到期日单列 unknown 不混进 current）。
      真缺口在**分批付款**：应付侧只有 `VendorBill.amountPaid` 一个**累计数字** ——
      余额能递减，但「分了几笔、哪天付的、谁付的、什么方式」全查不到。客户侧早有 Payment 流水表，两边不对称。
      **更要命的是并发丢账**：登记付款是 read-modify-write（前端读到已付 100 → 传 150），
      两个人同时各付 €50 都读到 0、都传 50，最终只记下一笔，另 €50 **不报错、不留痕，直接消失**。
      产出：`VendorPayment` 表（手写迁移 `20260812000003`，含存量回填）+
      `POST/GET /api/vendor-bills/:id/payments`（收**本笔金额**、服务端累加、`SELECT … FOR UPDATE` 行锁）+
      `lib/finance/vendor-settlement.ts`（付款落账/账龄分桶纯函数）+ 账单详情的付款记录表。
      实测**四笔并发各 €50 全部落地**（201/201/201/201，流水 4 笔，已付 €200），付清后的并发付款全被拒且金额没被穿透。
      `amountPaid` 从此是流水的汇总，`db:validate` 新增两条不变量守着（对称于应收侧的收款勾稽）。
      顺带修：付款凭证的幂等键原先拼 `${billId}-pay-${累计金额}`，同一账单先付 100 → 冲销 → 再付 100 会撞同一个 sourceId；改用流水 id。
      PUT 那条旧路径（含「全额付清」按钮）**保留但同样落流水** —— 否则 amountPaid 与流水两处真相。
      **账龄验收刻意不复用接口的 SQL**：脚本在 JS 侧用 `summarizeAging` 独立算一遍再逐格比对
      （同一段实现两边一比，"一致"毫无信息量——D5x 的假性通过就是这么来的）。实测 current €80 / d31_60 €120 / unknown €60 逐桶对上。
      ⚠️ **本条没做**：不做供应商对账单 PDF/邮件外发（G3 范畴）；不做付款冲销/退款的界面
      （表已支持负数流水，但冲销要定审批口径，属财务决策）。
      ⚠️ 存量回填**只补一笔汇总流水**，不编造分笔明细 —— 历史上分几次付的系统从来没记过，
      猜出来的分笔比一笔汇总更有害（看起来像真的）。note 里写明了它是回填。
- [x] **H2 透视引擎接入销售/采购各一页** — 20/20 通过 + 10 单测 + 浏览器实测 `e2f71f4`
      **⚠️ 清单对现状的判断不准确，本条的实际内容因此完全不同**：清单写「透视引擎只接了毛利分析一处、
      采购侧完全没有可组合分析」。实查：仓库里**早有一整套 Odoo 式报表** ——
      `lib/reports/*`（维度/度量白名单 + SQL 构造器）、`components/reporting/*`（工具栏/维度选择/
      度量选择/筛选/透视表/Excel 导出）、**三个页面**（销售/采购/物流）、`/api/reports/[type]`、
      以及三张 SQL 视图。**行列互换也早就有**。接口实测直接出数：销售 156 行 €556,797、
      采购 11 行 €1,275,177、物流 4 行。
      **真正的缺口是三个**：
      · **没有任何导航入口指向这三页** —— 全站没有一个链接过去。功能到不了 = 等于不存在。
        已加「报表分析」入口 + 三页共用的 tab 布局。
      · **下钻没做**（验收明写「行列互换与下钻可用」）。已实现：点行 → 选维度 → 子行插在父行下方。
        `lib/reports/drilldown.ts` 纯函数构造子请求（原查询 + 多一个维度 + 把这一行锁成筛选），
        **不新开接口** —— 下钻本来就是同一个查询，另开一个只会让两条路径口径慢慢跑偏。
      · **打开是一张空白提示**（「请选择维度和度量」）。有入口也得先猜两次才看得到数字。
        已给每张报表配默认视角（卖了什么 / 从谁那儿买 / 哪个司机跑的）。
      **实测查出并修掉的真问题**：
      · **输入校验一律返回 500** —— 选了不存在的维度/度量/筛选字段，用户看到「服务器错误」，
        而运维日志里一片 500 全是正常的输入校验，真故障被淹掉。新增 `ReportRequestError` → 400。
        （白名单本身是有效的，SQL 注入进不来 —— 实测 `o.id; DROP TABLE "Order"` 被拒。）
      · **时间维度下钻不能用 `between`**：sql-builder 的 between 是闭区间，而这些是 timestamp 列，
        拿日期串当上界会把末日整天切掉。改 `>=` + `<` 两条。**与 G1 对账单期间是同一个坑，没犯第二次。**
      **下钻的验收断言是「子行合计 == 父行」**，不是「能返回数据」：实测 €141,344.35 / 3335 逐格对上，
      浏览器里 14 个子行相加也正好等于父行。只断言「有返回」的话，锁错筛选照样通过。
      ⚠️ **查出一处装饰性配置（未擅自改）**：路由里 `ROLE_REPORT_ACCESS` 给 SALES 配了销售报表、
      给 DRIVER 配了物流报表，但这两个角色**一个 analytics.* 权限都没有**，请求在 gate 层就 403，
      根本走不到那张表 —— 看着像"业务员能看销售报表"，实际谁也看不到。补权限等于扩大数据可见面，
      属产品决策 → 见待决策 13。已在代码注释与测试用例里如实标注。
      ⚠️ **本条没做**：下钻只在**无列维度**的平铺表上可用（交叉表的行展开渲染复杂度高，
      且交叉表本身已能两维交叉）；不动毛利分析页那套独立的 `lib/analytics/pivot.ts`
      （两套并存是既有事实，合并属重构，不在本条范围）。
- [x] **H3 司机提成考核报表** — 27/27 通过 + 9 单测 + 浏览器实测
      产出：`lib/analytics/driver-commission.ts`（SQL 聚合 + 两个纯函数）/ `app/api/analytics/driver-commission`
      / `app/[locale]/classic/boss/analytics/driver-commission`（导航「司机提成」）/ `components/boss/driver-commission-tables.tsx`
      / `scripts/audit/driver-commission-test.ts`（`npm run test:driver-commission`）/ `tests/driver-commission.test.ts`
      / 迁移 `20260812000001_rbac_driver_commission_grant`
      验收达成：① 按司机 × 日/周/月汇总 ✓ ② 明细逐单给出件提成/固定费/比例提成三项构成并链到订单 ✓
      ③ **报表 €74.00 == `calcOrderCommission` €74.00 且非零** ✓（两套独立实现：接口走 SQL，脚本走 commission.ts）
      ④ 冻结与未冻结分列，未送达的单固定费不计 ✓ ⑤ 司机 403 / 老板 200 / 匿名 401 ✓ ⑥ 浏览器实点 ✓
      **顺带把假开关变成真的**：`analytics.commission.read` 是 I2 查出的 13 个「勾了不生效」之一，
      本条给它接上第一个判定点并随迁移发给 boss/operator，`detect-inert-permissions` 实测 **13 → 12**
      **三个只有浏览器/真数据才暴露的问题**（接口断言全绿时它们都还在）：
      · 明细日期显示成 `Wed Aug 05` —— date 交给前端后 `String(d).slice(0,10)` 截的是本地化星期串，改在 SQL 里 `to_char`
      · **点司机筛选「数字纹丝不动」** —— 汇总按 (driverId, driverName) 分组，筛选却只按 driverId；
        而实测库里 **BAO/AFZAAL/SEAN 三个人共用同一个 `Trip.driverId`**。筛选键改为与分组键一致，并补两条回归用例
      · 说明文字里的 markdown `**…**` 原样渲染成星号
      ⚠️ **`Trip.driverId` 与 `driverName` 不是一对一** —— 这是数据层的既有问题，正是 **C6** 要解决的，本条只在报表层绕开
      ⚠️ **上线后需通知 boss/operator 重新登录一次**：迁移 bump 了 permVersion，但**旧 token（无 `pv` 字段）不会被作废**，
        它们走 legacy 反查表，而新权限点不在表里 → 点开报表 403。这是 `lib/rbac/legacy-roles.ts` 的既定行为（宁可让人重登），不是缺陷
      ⚠️ **finance 刻意没给**：报表页在老板区（layout 只放行 BOSS+OPERATOR），单发 API 权限就是又一个够不着的开关；
        财务侧要核提成归 **C9** 一并决定
      ⚠️ 报表口径：提成归属**实际跑这趟的司机**（Trip），不是 `Order.driverSlotId`（改派后会分叉，20260708 那个坑），已写成断言
      ⚠️ 生产数据现状如实呈现：种子里 41 单**件提成全 €0.00**（历史订单行无 `commissionPrice` 快照）、**0 单冻结**——
        这正是 B2 查出的三缺口，报表把它显示成「待冻结 41」而不是抹平成 0
      **B2 欠下的受控实验已补**：走真实 HTTP 下单，`OrderLine.commissionPrice` 快照 3/3 写入 → **链路没有在漏**，
        缺的是存量回填（B2 建议第 2 步，仍待决策）
      ~~**开工前现状核实（20260812）**~~：
      · 计算入口 `lib/commission.ts` 完整；冻结触发点在 `trips PUT status=COMPLETED`（verify 那处是历史空转，订单当时还不是 COMPLETED）
      · 物流分析页有司机行程/收款/差异，**没有提成**；可组合报表 logistics 的 `driver_commission` 只到 `Trip.driverCommission` 合计，看不到构成
      · **测试库数据为零**：299 单 / 36 COMPLETED / **冻结 0 单**；1540 个订单行只有 **1 行**有 `commissionPrice` 快照；客户提成率 1 个、固定费 0 个
      → 所以「先建报表」会重演 B2 的**假性通过**（两边都是 0 也叫一致）。本条必须连数据一起解决
      验收：① 按司机 × 周期出汇总（单数/实送额/件提成/固定费/比例提成/合计）② 明细逐单可见构成并能追到订单
      ③ **报表合计 == 逐单 `calcOrderCommission` 重算之和，且金额非零** ④ 冻结与未冻结分开呈现，不把没冻结的当已发
      ⑤ 司机只能看自己的，运营/老板/财务看全部 ⑥ 浏览器实点
- [x] **C4 任务接收链路实测** — 15/15 通过 + 6 单测 + 浏览器实测（司机端与运营端各一遍）
      产出：`scripts/audit/driver-task-visibility-test.ts`（`npm run test:driver-tasks`）
      / `lib/row-scope.ts` 新增 `driverRowScope` + `withDriverScope` / `tests/row-scope.test.ts` 补 6 例
      验收达成：① 分配 + 确认出发 → 司机账号刷新后看到该单 ✓ ② 未指派给他的看不到 ✓ ③ 改派链路正确 ✓
      **查出并修掉两个越权（本条最重要的产出）**：
      · `GET /api/trips` 的 `driverId` 是**查询参数**，司机端靠它过滤自己的行程 —— 但换一个 id 就能看别人的，
        **不传参数则返回全公司行程**（客户地址、金额、收款一并带出）。与 20260802 `/api/customers` 同类：前端过滤不是隔离
      · `GET /api/trips/[id]` **连 `withAuth` 都没有**，拿到 id 就能逐条读走别人的行程。
        列表挡住而详情没挡等于没挡（行程 id 在司机端 URL、交账页、日志里到处都是）
      修法：`driverRowScope` —— 只挂 DRIVER 的账号一律只看自己的，兼任 OPERATOR/BOSS/FINANCE/DISPATCH 的不隔离；
      条件用 AND 并入 where（不能写 `where.driverId=`，会被同名查询参数覆盖 —— 20260806 那次就是这么被绕过的）；
      详情越权返回 **404 而非 403**（403 等于告诉对方"这个 id 确实存在"）
      **另修一个 500**：出发后改派，`WaveDispatchedError`（"该批次已出发，不能再分配"）被正确抛出，
      但 `orders/[id]/batch` 只 catch 了 `WavePickLockedError` → 落进 500「分配批次失败」。
      调度台的 assign/unassign 两条路由早就捕获了，唯独走 `assignOrderToWave` 的这条漏了 ——
      同一条规则，从调度台拖拽有清楚提示，从销售单列表改派是无信息的 500
      **司机端每次打开打出 7 个 403**：`hydrate()` 无差别八连发（orders/waves/invoices/customers/pricelists/stock-moves/purchases），
      司机一个都够不着。权限是对的，浪费的是请求本身（还占 2 vCPU 的并发）。改为按会话可达性过滤，
      判定用 middleware 那张表；浏览器实测司机端与运营端**控制台均 0 错误**
      ✅ **主数据缺口已补（20260812，用户要求）**：3 个 `DriverSlot`（AFZAAL/BAO/SEAN）的 `userId` 原本全为空，
      而 `createTripFromWave` 的司机身份取自 `slot.userId` → 生成的 Trip `driverId=null` → 司机端一条也查不到。
      新增 `scripts/seed/bind-driver-slots.ts`（`npm run db:bind-drivers`）：按档位司机名找/建 DRIVER 账号并绑定，
      **幂等 + 默认预演**（不加 `--apply` 一个字都不写）。测试库 3/3 已绑（afzaal/bao/sean@veggie.com），
      司机配置页实看已显示绑定。新账号是弱口令 + `mustChangePassword=true`，首次登录除改密外什么都调不了 ——
      **绝不给司机一个我们知道密码的长期账号**
      · 已接进 `db:bootstrap` 第 5/8 步 —— 这是第四次撞上「只被人手配过、任何脚本都不填」的表
        （前三次：RBAC 角色权限、采购品类分组、SQL 视图），重建库不会再缺
      · ⚠️ **生产环境仍需执行**：脚本支持 `--allow-remote --mapping "BAO=真实邮箱,..."`，
        但**真实邮箱必须客户提供**，默认拼出来的 `bao@veggie.com` 不能用在生产上。见下方「待客户提供」
      · C4 测试脚本造的档位现在会自我归档（归档而非删除 —— `PickingWave.driverSlotId` 还指着它们）
      ⚠️ 销售单 PUT 那条改派路径**本来就对**（有更早的按订单状态判的守卫，409 且文案清楚），
      本轮给它补的 catch 是走不到的兜底 —— 如实记下，不把"本来就好的"算成修复
- [ ] **C5 电子签名验收** — 能签能重签能保存，后台可查
- [ ] **C6 司机实体统一校验脚本** — 全量订单司机来源不一致数 = 0，接进 `db:validate`
- [ ] **C7 车辆调度 + 地图路线（Web）** — 一个波次能画出完整路线并给出里程/时长；无坐标客户有降级提示
- [ ] **C8 司机每日回传（Web）** — 现金/订单总额/退货数/换货数 四项落库，重复提交防重
- [ ] **C9 财务当日确认货款 + 历史欠款冲抵** — 确认动作有独立权限；确认后司机不可改；超收部分能冲抵到客户欠款并留痕
- [ ] **C10 司机对账状态统计** — 未提交/待确认/已确认/有差异 按司机按日汇总，差异标红，可导出
- [ ] **I1 权限密码测试验收** — 弱密码被拒、改密即失效、权限变更强制重登、**重置邮件真能收到**（依赖 Z2）

---

## W3 · 被用户决策阻塞（不得开工）

> **用户决策 20260812：多仓（E1/E2x）、Sage（G3）本期先忽略；D7 用户表示「口径没理解」，
> 已在对话中解释并提出绕开方案（见下），等回复。其余先往下做。**

| 任务 | 阻塞原因 | 20260812 决策 |
|---|---|---|
| E1 多仓库划分 | **客户实际有几个物理仓**（现仅 Zone 温区，无 Warehouse 实体） | ⏸ **先忽略** |
| E2x 移仓实现 | 规划已出（`docs/20260811-stock-transfer-mvp.md`）；待客户确认「几个物理仓 / 要不要审核 / 是否影响成本」三问后选方案甲或乙 | ⏸ **先忽略**（随 E1） |
| D7 缺货人工跳过 | 「在途」判定口径（PO 已审核？已发货？预计今日到？） | ❓ 用户未理解口径 → 已提**绕开方案**：不做自动判定，只做「人工跳过 + 留痕」，界面把该商品的在途采购单（PO 号/状态/预计到货日/数量）摊开供人判断。这样无需现在定死口径。**等用户确认后可开跑** |
| G3 收付款管理 | 本期是否只做演示、Sage 对接时点 | ⏸ **先忽略** |

## W4 · 本期不做（决策已定 20260811）

| 任务 | 结论 |
|---|---|
| C1 APP 形态选型 | APP 本期不做，先补完 Web 端 |
| C2 安卓开发者账号 | 随 APP 一并挂起 |
| C3 内测包分发通道 | 随 APP 一并挂起 |

---

## 待决策（需用户答复才能推进）

1. ~~**测试库**~~ → ✅ 已定 20260811：**本机 PostgreSQL + 迁移 + 种子**
2. ~~**司机端 APP 形态**~~ → ✅ 已定 20260811：**APP 本期不做，先补完 Web 端 C4~C10**
3. **多仓库**：客户实际几个物理仓？（阻塞 E1/E2）
4. **缺货「在途」口径**（阻塞 D7）
5. **Sage 对接时点 / G3 是否只做演示**
6. **是否同意新增 `Order.source` 字段**（已建议同意；A3/A4 按"同意"推进，若否则回滚）
10. **13 个「假开关」权限点怎么处理**（I2 查出）：逐个决定「补上判定」还是「从权限目录删除」。
    在处理之前，涉及这几个动作（确认订单/取消订单/调度台访问/作废发票/供应商增删改等）的
    限制**不能只靠勾选权限点**。清单见 `npx tsx scripts/rbac/detect-inert-permissions.ts`
9. ~~**`qtyOnHand` 的权威来源**~~ → ✅ **已定 20260811：不补历史流水，定起算日**
   - 起算日之前的库存视为**期初余额**，补一笔期初 `ADJUSTMENT` 流水对平
   - 起算日之后**严格走流水**，`qtyOnHand` 必须恒等于 Σ StockMove
   - 落地时机：✅ 已由 Z5 落地（`ensureOpeningStock`）；E5/D6 的验收标准以此为准
   - 起算日已取 **20260805**（droplet 切换日）写在 `scripts/seed/ensure-opening-stock.ts` 的 `OPENING_DATE`
   - ⚠️ 若客户有明确的系统启用日，改这一个常量即可

11. **超收要不要拦？**（E5x 查出）：收货数量大于采购订单数量时，系统当前**按实收入库不拦**。
    合理但需业务确认 —— 供应商多送了到底算收下还是退回。确认前 `scripts/audit/receipt-stock-test.ts`
    的该用例只排除「按订购量入库」这一种错误行为，不锁定另两种。

13. **报表的角色可见性**（H2 查出）：路由 `ROLE_REPORT_ACCESS` 里 `SALES:['sales']`、
    `DRIVER:['logistics']` 两条**当前够不着** —— 这两个角色没有任何 `analytics.*` 权限，
    请求在 gate 层就 403。要么给它们补 `analytics.report.generate`（= 扩大数据可见面，
    且业务员那条还会叠加行级隔离 `sales_user_id = 自己`），要么删掉这两行承认只有
    运营/老板/财务看报表。**在决定之前不要对着那张表得出「业务员能看销售报表」的结论。**

12. **真实供应商询价单 PDF**（F2 需要）：客户提供 3 份真实文件后，放进 `test/` 并在
    `scripts/audit/pdf-extract-test.ts` 里各加一例复跑。当前解析器已在「客户自家单据 +
    系统生成单据」两类规整电子版上验证通过，但**供应商的真实排版没见过**，
    很可能需要补表头关键词或列序规则。

> **执行授权（20260811）**：用户已明确「后续不用问，都继续」。此后各周期不再逐条征询，
> 遇到可自行判断的选择就按最合理的默认值推进并在台账写明假设。
> **但以下仍必须停**：CLAUDE.md 第四节清单（删生产数据、GCP 部署、覆盖用户重要文件等）、
> 同一问题连续 2 次未修好、以及需要客户提供外部事实才能定的事（多仓库几个仓、Sage 对接时点等）。
7. ~~**UI 走查是否改用生产构建**~~ → ✅ 已验证可行（Z8a），但要用 `node .next/standalone/server.js` 起，不能用 `next start`
8. **Z8b 要不要继续查**：受保护页面进不去，已连续 2 次未修好按规矩停手。选项：(a) 我按台账里列的三个方向继续查 (b) 先跳过，UI 走查押后，我去做 W1 剩下 9 条

---

## 硬停止条件

- 台账已无未完成条目 → 输出总结
- 同一个问题连续 2 次没修好 → ⛔ 停下来问用户，不做第 3 次
- 撞上 CLAUDE.md 第四节清单 → 按该节处理
- 台账与实际代码状态对不上 → 停下核对
- **任何需要写生产库的动作** → 停

---

## 测试账号（本机 veggie_test，密码统一 `LocalTest2026!`）

| 角色 | 账号 |
|---|---|
| OPERATOR | operator@veggie.com |
| BOSS | boss@veggie.com |
| FINANCE | finance@veggie.com |
| WAREHOUSE | warehouse@veggie.com |
| SORTER | sorter@veggie.com |
| DRIVER | driver@veggie.com |
| RESTAURANT | restaurant1@veggie.com / restaurant2@veggie.com |
| SALES | sales@veggie.com（周期 2 新增，roles = OPERATOR+SALES） |

> 9/9 均已实测可登录。注意登录接口字段名是 `email` 不是 `username`，且有 IP 限流——
> 连续试错会拿到 `RATE_LIMIT` 并需等约 50s。

启停测试库（`brew services` 在沙箱内不可用，必须用 v17 显式路径，否则会被 PATH 里的 v14 二进制拒绝）：

```
/opt/homebrew/opt/postgresql@17/bin/pg_ctl -D /opt/homebrew/var/postgresql@17 \
  -l /opt/homebrew/var/log/postgresql@17.log start
```

## 部署记录

| 日期 | 内容 | 结果 |
|---|---|---|
| 20260812 | **W1+W2 累计 89 个提交一次性上生产**（run `31590792782`，`ec05497`）| ✅ 成功 |

细节（下次部署前先读这段）：
- 链路：`push main` → GitHub Actions 构建镜像 → SSH 客户服务器 → `prisma migrate deploy` → `compose up -d` → healthcheck
- **4 个迁移全部应用**：`order_source` / `stock_move_loss_attribution` / `po_actual_arrival` / `vendor_payment`；
  日志显示 73 个迁移中这 4 个为新增，其余已在库。**迁移链在存量生产库上是通的**
  （Z6 查出的"空库重放不了"只影响从零重建，不影响增量部署——本次实证）
- 健康检查：`{"status":"ok","db":"ok"}`，容器重建到就绪 ~5 秒
- **生产实际访问已核对**（不只看工作流绿灯）：`https://www.johnstonebros.ie/enter` 200 且登录页正常水合；
  `/api/health` 200；`/api/orders` 匿名 401；**新增的 `/api/vendor-bills/*/payments` 匿名返回 401 而非 404**
  → 说明新路由确实上线且鉴权生效
- ⚠️ **域名解析已指向 droplet**：`167.99.86.19` 上的 nginx 把 http 301 到 `https://www.johnstonebros.ie`。
  记忆里"流量还没切、客户可能仍在用 Cloud Run URL"这条**已过期**
- ⚠️ 本次**未登录生产系统做写操作**，也未在生产库造数据。G1 验收里"生产库真的出现第一张 Statement"
  仍需在客户环境由业务实际操作一次才闭环

## 周期日志

| 周期 | 任务 | 结果 | commit |
|---|---|---|---|
| 1 | Z1 起本机测试库 | ✅ 完成，但 `migrate deploy` 走不通改用 `db push`；途中发现并修复 `db:seed` 已静默产出垃圾数据一个月，新登记 Z4/Z5/Z6 | `e5c5793` |
| 2 | Z3 各角色账号可登录 | ✅ 9/9 通过（补了缺失的 SALES 账号 + 种子 roles[]）；浏览器登录不通（未水合）另立 Z8，RBAC 表空另立 Z7 | `0f731f2` |
| 3 | Z8 浏览器可用性 | ⚠️ 一半：Z8a 水合问题用生产构建解决（浏览器登录已真正跑通）；Z8b 中间件看不到 cookie，2 次未修好触发硬停止 | 无代码改动（`.env.test` 被 git 忽略） |
| 4 | B3 快捷键公共机制 | ✅ 建立三层（纯函数 / hook+浮层 / 两页接入）+ 15 个单测；更正了「散在 9+ 组件」的原判断——那层其实不存在。`?` 浮层的浏览器实测欠着，等 Z8b | `8e98c0d` |
| 5 | E3 库存守恒体检 | ✅ 推翻「895 个不守恒」（实测 7 个），但查出更严重的：出库流水只覆盖 0.4% 的已确认订单，库存字段与流水基本脱钩 | `22ddce7` |
| 6 | B2 定价提成比对 | ✅ 定价零例外；提成对 15 万单产出为 0（订单行快照仅 0.01%）。**并查明 Z8b 根因是 Z7，更正周期 3 的错误结论**；Z7 升级为头号阻塞 | `02431b2` |
| 7 | Z7 RBAC 铺底 | ✅ 19 角色/181 权限点/20 绑定，位图非零，带鉴权 API 恢复 200（driver 403 属正确区分）。**W2 全部 30 条解锁**，Z8b 随之关闭；但暴露出 Z9（所有页面路由 307 自循环），2 次未修好停手 | `d95e328` |
| 8 | Z4 事件种子修复 | ✅ 更正「幂等问题」的原判断——实为首次运行就必坏。共享标记写进唯一列，7 个使用点一并改前缀语义。测试库首次产出 210 单/124 发票/1455 流水，二次运行逐项一致，11 项不变量在**有数据**的情况下全绿 | `5ae4ffc` |
| 9 | Z5 库存底数 | ✅ 期初余额口径落地，129→1677 商品有库存，守恒全绿且幂等。期初流水带 OPENING-BALANCE 标记可与业务流水区分 | `9148fb2` |
| 10 | Z6 迁移链基线 | ✅ 查明**迁移链缺初始迁移**（核心表无任何 CREATE TABLE），migrate deploy 建不了空库。固化 `npm run db:bootstrap` 并实测全绿；baseline/squash 因涉生产 resolve 未擅自执行 | `4be4412` |
| 11 | A4 订单来源字段 | ✅ 门户单 source=PORTAL 落库，列表可筛选、非法值不 500。**并查出 pkill 匹配不上 standalone 进程**——周期 3~7 的「重启」全是空操作，Z9 失败次数据此从 2 次修正为 1 次 | `c7ffb9d` |
| 12 | A3 门户单端到端 | ✅ 10/10 断言通过（建单/状态/进列表/来源可识别可筛选/行数金额定价全对）。走真实 HTTP 接口，非直接写库 | `f9899e8` |
| 13 | Z9 + Z10 | ✅ Z9 根因是我自己加的 `HOSTNAME=127.0.0.1`（非应用缺陷），拿生产站点做对照定位。解除后连带验掉 A4 列/B3 浮层/A3 视觉三项欠账，**并揪出 B3 引入的 React #310 详情页白屏**（三道关卡全拦不住，只有浏览器能发现）| `c93a341` |
| 14 | A1 常购清单走查 | ✅ 交互均有反馈、无死按钮、空状态存在。修掉购物车孤立分隔点；查出 P1「下架商品静默消失且名额不递补」等 5 项记入报告 | `61425bd` |
| 15 | A2 账期定价走查 | ✅ 账期与成交价一致。**查出并修复 P0：门户下单税率 100% 为空导致开票少计税**（服务端"权威定价"只权威了价格）；并给购物车与订单详情补上「不含税/税额/含税应付」 | `d89bb8a` |
| 16 | B1 重复/缺货提醒走查 | ✅ 三种场景全过，提醒质量高（行级标记 + 可继续保存）。修复种子税率量纲（小数 1444 行→0，并同步下游 5 处计算）；新登记 Z11：生产库 99.4% 行税率为 NULL | `de89dbd` |
| 17 | D2 四类单据打印走查 | ✅ 四类全打通、金额逐笔对上、**司机名历史坑未复发**（不一致数 0）。修复配送单据打成空白（种子 restaurants 字段名与应用不一致）与发票行级税率被整数化（14% vs 13.50%）| `2693df1` |
| 18 | D5 打印场景矩阵 | ✅ 23 例（4 单据 × 7 状态 × 4 筛选 + 边界），当前 21 通过 0 失败 2 未获验证。**自我纠错**：第一版把 3 个「跳过」记成了通过，已改为分开计数；并让种子留出 29 张 COMPLETED 态订单 | `4be3810` |
| 19 | E5 收货→库存测试 | ✅ 6 场景 + 2 守恒断言。**测试逼出真 bug**：收货损坏品写正数 SCRAP 而库存不动，破坏头号不变量；与 /api/scrap 的符号约定相反，因种子从不造损坏收货而一直没暴露。改记 IN+SCRAP 两笔净额 0 | `44baf05` |
| 20 | E2 移仓 MVP 规划 | ✅ 两版方案。核心判断：温区是**商品属性**不是库存维度，系统无法表达「A 仓 30 件 B 仓 20 件」，故移仓是「要不要给库存加维度」而非补界面。建议先做轻量版（1-2 天），三件事待客户确认 | `de6d683` |
| 21 | G4 成本核算方案 | ✅ 结论与预期相反：**算法本来就对**（加权平均 + FIFO 已实现），缺的是数据（批次 45 个 / SCRAP 6 条 / 凭证 0 张）。建议不改算法先补数据。**两处判断实查后自我更正**，报表 MVP 从三张收窄到一张 | `db2fc15` |
| 22 | H1 数据中心覆盖矩阵 | ✅ 实查 17 页 + 16 接口。需求点名的四个日销售指标**全部已实现**且共用同一份缺货计算；三大模块里经营看板与客户分析**已完整**，**商品分析是唯一真缺口**（数据都有，缺以商品为主体的入口）| `2dc582a` |
| 23 | I2 权限操作指南 | ✅ 照真实界面写的 4 篇进系统内帮助页。**假开关实测 13 个而非台账记的 8 个**，已固化检测脚本并在指南中如实提示。**W1 至此 13/13 全部完成** | `f980baa` |
| 24 | I3 大小单位全链路 | ✅ 8/8。修 2 个真 bug（收货不换算数量、成本未同步换算致虚高 12 倍）+ 补 uomName 服务端快照。**发现 1677 个商品模板基准单位全为空**，该机制此前无数据可流经 | `7720876` |
| 25 | D4 拣货单拆箱 | ✅ 11/11。拆箱纯函数+接进模板，浏览器实看纸面；**顺带查出打印 CSS 只有页边距**，多页会截行且第二页无表头，一并补。测试数据自身不守恒(直塞 qtyOnHand)当场修掉 | `fb884de` `468a8b3` |
| 26 | D1 打单工作台 | ✅ 10/10。「已打印」由 localStorage 改服务端聚合（数据本就在 ActionLog 里）；补重打二次确认与「还剩 N 批未打」。**安全绳测出旧 token 403 缺口**；查明 standalone 静态资源漏拷才是此前 UI 验不了的原因 | `db11ff3` |
| 27 | D3 打印三维筛选 | ✅ 23/23。客户/商品两维新增，与既有线路维正交可组合；**预览与打印共用同一段纯函数**而非各写一遍。补上拣货单/汇总单一直缺的「按筛选打印」纸面提示（此前只有送货单/销售单渲染 notice）。顺带修下单接口客户名只认客户端传值、不传就写死「未知餐馆」 | `8c2b3c0` |
| 28 | D6 缺货批量改单/转单 | ✅ 25/25。先定死拣货锁优先级（减量放行/加量拦截，推翻已过期的「彻底锁」记忆）；新增批量接口带必填原因与转次日草稿单。**并查明 E3 那 7 个不守恒商品的成因**就是缺货改量路径只改 qtyOnHand 不写 StockMove（生产副本 6/134 商品对得上） | `4191120` |
| 29 | D8 四指标口径 | ✅ 13/13。口径收进 metrics.ts 并补 14 个单测；**查出三处错位**：快照按进程时区切日（1909 单受影响）、缺货率序列整体偏一天（查今天返回昨天）、客单价公式散在三处。**并补上 db push 跳过的视图/扩展** —— 全新库缺 v_lot_daily_cost 会让分析中心整块 500 | `10da755` |
| 30 | D9 销售矩阵日/周 | ✅ 18 单测 + 浏览器实测。先核实现状：星期表与库存/ATP 都已有，真缺口是「矩阵导不出去」。加日/周切换 + 库存/ATP 列 + 矩阵 CSV（用屏幕同一份结果对象）。按日与按周两种切法互相印证（周三 114 = 94+20）| `afd77d7` |
| 31 | E4 损耗环节归因 | ✅ 18/18。**清单说「录入界面缺失」不准确——界面在，但选品框永远是空的**（前端按接口不返回的 template.type 过滤），改服务端筛。新增 lossStage/lossReason 两列把环节从原因里拆出来，看板可按环节拆；顺带把 45 件「未归因」正确归到「收货」| `5502421` |
| 32 | E5x 收货用例执行 | ✅ 17/17。原 6 场景全过、无失败项；但那 6 例只守 qtyOnHand/StockMove，**批次与成本这条腿一直没人守**（G4「批次仅 45 个」的由来），故扩到 17 例并补 I3 按箱收货回归。顺带补上 E4 漏掉的收货损坏结构化归因 | `126b71c` |
| 33 | E6 收货关联+预计到货 | ✅ 12/12。一键收货本已存在；补预计到货日与偏差徽标；**「未关联收货」不能去 GoodsReceipt 表里找**（非空外键，永远 0 条），改查绕过收货单的入库流水。实测中修掉两处：计数被上限截断（200→200 恒不变）、期初余额淹没信号（1611 → 29 笔）| `83c5cd8` |
| 34 | E7 实际到货日回写 | ✅ 17/17。首次/收齐两个日期都存，不为「按哪个」卡住等确认；准时率口径从严（只算已收齐、null≠0%）。**写回按 MIN/MAX 取而非「先到先填、后到覆盖」**——补录更早的收货单时前者才对；迁移带幂等回填（1→43 张）| `ca7ec3f` |
| 35 | D5x 打印用例执行 | ✅ 30/30。原 23 例本就 0 失败，但**筛选那组是假性通过**（断言只判 isArray，什么都没筛掉也算过；实测「按线路」返回的是整日全量）。每一维改成断言「确实筛窄了」，并补上矩阵自称覆盖却没测的按客户/按商品与「4 类单据 × 筛选」渲染 | `7d81ffc` |
| 36 | F1 采购建议链路 | ✅ 16/16。界面与算法本已存在，跑通后查出三个真问题：**全新库 CategoryGroup 为空导致整块功能返回空**（第三次撞上「只被一次性脚本填过的表」）、**转单生成 DRAFT 而在途只算 CONFIRMED → 第二天重复建议**、采纳后金额没跟着数量改。四个输入造成已知值后断言建议量恰好 = 20 | `138ba3c` |
| 37 | F2 PDF 确定性解析 | ✅ 9/9。把「丢给 AI 出 JSON」换成纯函数解析（需求明写不上 AI）。**三个坑全是实测撞出来的**：standalone 上 pdf.worker.mjs 缺失导致一上传就 500（dev 看不见）、真实 PDF 一行折成三行导致单价恒 null、表头两词挤一格导致把行小计当单价。另修好 no-direct-cloud-sdk 守卫的假阳性（注释提及被判违规）| `fadd266` |
| 44 | C4 任务接收链路 | ✅ 15/15 + 6 单测 + 浏览器实测。**查出两个越权**：`/api/trips` 不传 driverId 返回全公司行程、`/api/trips/[id]` 连 withAuth 都没有；加 `driverRowScope` 补上行级隔离。另修「出发后改派」掉进 500（闸门有，错误没接上）与司机端每次打开打 7 个 403。**并查出主数据缺口：3 个 DriverSlot 的 userId 全为空**，司机因此看不到任务 —— 要客户去绑 | 本次 |
| 43 | H3 司机提成考核 | ✅ 27/27 + 9 单测 + 浏览器实测。**把 `analytics.commission.read` 这个假开关接上了第一个判定**（13→12）。三个坑全靠浏览器/真数据才现形：日期显示成 `Wed Aug 05`、**点司机筛选数字不动**（分组按 (id,name) 而筛选只按 id，实测三个司机共用一个 driverId）、markdown 星号原样渲染。夹具第一版直塞 qtyOnHand 破了守恒，改走期初余额口径 | 本次 |
| 42 | H2 可组合报表接入 | ✅ 20/20 + 10 单测 + 浏览器实测。**清单判断不准确**：Odoo 式报表（销售/采购/物流三页 + 白名单 + 行列互换 + Excel 导出）早已存在且能出数，真缺口是**全站没有一个链接指向它们**、下钻没做、打开是空白提示。补入口/tab/默认视角 + 下钻（子行合计==父行，实测逐分对上）。修掉「输入校验一律 500」；时间下钻用半开区间，没重蹈 G1 的 between 坑。查出 SALES/DRIVER 报表可见性是够不着的装饰性配置，未擅自扩权 | `e2f71f4` |
| 41 | G2 供应商结算 + 分批付款 | ✅ 20/20 + 7 单测 + 浏览器实测。自动生成账单与账龄本已存在；真缺口是应付侧只有一个累计数字、且登记付款是 read-modify-write（**两人同时各付 €50 会静默丢一笔**）。新增 VendorPayment 流水表 + 增量付款接口 + 行锁，实测四笔并发一笔不丢；db:validate 加两条应付勾稽不变量。账龄用**独立实现**在 JS 侧重算比对，不复用接口 SQL | `7dfed4d` |
| 40 | G1 客户对账单 + 司机现金核销 | ✅ 28/28 + 11 单测 + 浏览器实测。**界面与核销逻辑本已存在**，真缺口是生成算法四处错位（末日整天被切掉 / 日界按 UTC / 销售口径用 createdAt 而非 confirmationDate / **首张对账单期初恒为 0**——正是本任务场景）+ 明细逐笔对不上。新增 withDetail 明细与服务端核对，重复生成 409。浏览器实测揪出 `Payment.createdBy` 两条路径一个写人名一个写 userId | `cc6da33` |
| 39 | F4 采购质检 MVP | ✅ 26/26 + 12 单测 + 四个界面实点。质检记录随收货行落库、**不新建表**，采购单与批次追溯都是派生；结论由体检值派生而非人工另填。**拒收与报废刻意不同**：不入库、不写流水、不计 receivedQty，PO 因此保持未收齐（浏览器实证：3 个待收货 → 1 个）。农残超标未拒收强制写让步理由 | `5c0773a` |
| 38 | F3 采购全生命周期 | ✅ 21/21。全链实打（含用司机账号验 403 且状态未被改动）；**补上原本不存在的「采购退货」**。⚠️ 自己写的退货路由当场造出批次守恒违例——只扣 Lot 不记 lotId，**商品级守恒照样绿、只有批次级露馅**；同一隐患在 /api/scrap 也有，一并修 | `cdfe6ab` `5d0b94a` |
