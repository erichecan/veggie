# veggie 部署效果闭环日志 · 2026-09-25

## 会计核销重构：合并司机交账/对账为单页 + 补齐原型对照缺口（2026-09-24 开发，2026-09-25 凌晨部署，2 次提交）

**本次改动想达成什么**

砍掉从未真正走完过一次流程的司机交账（settlements）/司机对账（driver-reports）两条流水线，把「钱」和「单」合并进 `/classic/accounting` 单页：钱板块系统自动算今日应收、会计核对后确认即真入账；单板块扫码两步核销 + 退回核实第三态。详见 `DEV-PLAN.md`、`DEV-REPORT.md`。

第一次提交（`3e69195`）前跑了 `/code-review high`（8 视角）+ `/security-review`：修了 1 条致命级发现（`/api/orders/bulk` 的 `withAuth` 内层闸门漏改，FINANCE 角色实际进不了核销功能——这是本次要交付的核心场景）+ 3 条一般发现，1 条核实生产 `Payment` 表当前 0 条记录无需回填，5 条记入技术债。

第二次提交（`fdd322c`）：用户拿已确认的原型（`https://claude.ai/artifact/Qb5CtaHvKeaBjkH7P85LCm`）逐项核对生产页面，发现单板块统计卡片漏做了「待确认」一项（原型 6 张卡，生产只有 5 张）——执行疏漏，非设计变更，已补回。

**技术观测（我负责）**

| 项 | 结果 |
|---|---|
| npx tsc --noEmit | ✅ 0 错（两次提交均复跑） |
| npm run build | ✅ 新路由 `GET/POST /api/accounting/driver-cash(/confirm)` 在清单中，旧路由（settlement/driver-reports）已消失 |
| npx prisma migrate status | ✅ up to date（2 个新迁移） |
| npm test | ✅ 910/913（1 个失败为共享开发库既存 `pricing-override.test.ts` 缺 `ABCT` 夹具，与本次改动无关，两次提交前后一致） |
| role-reachability 快照 | ✅ 已按权限闸门修复重新生成，`POST /api/orders/bulk [FINANCE] n → y`，理由写入提交信息 |
| 生产库只读核实 | ✅ `Payment` 表 0 条（marker 改名不影响历史数据）；`_prisma_migrations` 确认两个新迁移均已应用 |
| GitHub Actions Deploy to droplet | ✅ 两次均绿，`3e69195`（8m49s）、`fdd322c`（6m23s） |
| 生产容器状态 | ✅ 两次部署后 `docker ps` 确认容器镜像 tag 与提交 SHA 一致、healthy；近 5 分钟日志无 error/exception |
| 浏览器实测（本地 + 用户在生产端核对原型） | ✅ 页面渲染正常、无控制台报错；统计卡片补回后顺序与原型一致 |
| 提交 | `3e69195 feat(accounting): 会计核销重构`、`fdd322c fix(accounting): 补回统计卡片漏做的「待确认」一项` |

**定性观测（用户/客户负责）**

用户已亲自对照原型核对完毕，确认：
1. 核心交互（钱板块按司机汇总确认即入账、单板块扫码两步核销 + 退回核实、批量操作、统计卡片点击筛选）与原型一致
2. 唯一缺口（待确认卡片）已指出并验证补齐
3. 两处刻意偏离原型的设计决策（钱板块取消撤销确认——因为改成真入账；表格列比原型多——原型是假数据没有这些字段）已获知悉，无异议

**状态**：已达成——功能上线、致命权限缺陷已修复、原型对照缺口已补齐并二次部署验证

---

**附：本次顺带发现但与本次功能无关的事项，记录不处理**

核对部署效果时用只读 SQL 发现生产库最新订单 `deliveryDate` 为 2026-09-19，此后到 2026-09-25 无新订单（总量 149,930 条）。用户确认：现在是测试阶段，没有真实订单流入是预期状态，不是本次改动引入的问题，无需处理。

---

## 遗留：2026-09-23 三条「待观测」记录尚未收到用户/客户的定性反馈

以下三条部署（见 `docs/20260923-veggie-deploy-log.md`）技术侧已达成，定性侧仍待用户去问真实业务角色，本次会话未催办，留待下次部署会话开头按规矩处理：

1. 退换货流程补缺口（仓库核实 + 销售审核权限 + 司机签名）——需司机/仓库/销售各找一位实际操作过一次给反馈
2. 财务入口合并（会计 + 发票 + 会计 tab 三合一）——需用户及日常经手开票的同事确认操作路径顺不顺手
3. 波次标记完成回填 deliveredQty/订单状态/提成/发票——需调度/运营同事确认点「标记完成」后提成列、草稿发票是否符合预期；技术侧因当前测试阶段无真实波次标记完成动作，暂无法用生产数据复核
