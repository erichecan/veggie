# 交接：销售/会计模块 9 项修复（2026-06-20）

> 上个会话因终端输出通道污染（Bash/Read 返回乱码）暂停。代码改动经 Edit/Write 原子写入，**应已可靠落盘**。
> 重启后第一步：跑 `npx tsc --noEmit`，清掉残留的 TS6133 未使用变量，再 `next build` 验证，然后提交+部署。

## 约束
- **不要跑本地 preview**：用户 DB 在云端，本地无法测试。只用 `tsc` / `next build` 验证。

## 9 项需求与处理

1. 销售单详情页移除「生成拣货单」按钮（wave 详情页保留给配送调度中心）→ 改 `app/[locale]/classic/operator/orders/[id]/page.tsx`：删 `generating` state、`handleGenerateWave`，新增 `handlePrint`。
2. print 页条形码不显示（根因：React 不执行 `dangerouslySetInnerHTML` 注入的 `<script>`）→ `app/[locale]/classic/print/[id]/page.tsx` 末尾 return 改为 `<iframe srcDoc={html}>`。
3. print 后列表打印状态不变 → 详情页 Print 按钮 `handlePrint` 调 `POST /api/orders/[id]/mark-printed {type:'SALES'}` 后 `load()`。
4. 发票号点击 500 → 无法本地复现（云端 DB）。最可能是 `Invoice.lines`(Json) 旧数据缺字段导致 `.toFixed()` 客户端崩。已在 `app/[locale]/classic/operator/invoices/[id]/page.tsx` 对 `inv.lines` 用 `(inv.lines ?? [])` + 每个数值 `Number(x ?? 0)` 加固。**部署后若仍 500，需查 Cloud Run 日志确认是否 API GET 抛错。**
5/9. 发票/采购列表 Mode→Edit 死按钮 → 移除 `permanentActions` 里的 Mode/Read/Edit + 删 `isReadMode` state。文件：`invoices/page.tsx`、`purchases/page.tsx`。
6. 新建供应商账单弹窗太小 → 仅在 `vendor-bills/page.tsx` 新建账单 Dialog 上加 TODO 注释，不改尺寸（用户要求后续讨论）。
7. 新建供应商账单需 PDF/Excel 导入+字段映射（复用采购模式）→
   - 新建 `lib/import-parser.ts`（抽取 PDF/Excel/CSV 解析+商品模糊匹配，采购单 import 也已改为复用它，DRY）。
   - 新建 `app/api/vendor-bills/import/route.ts`。
   - `vendor-bills/page.tsx` 加 Import 按钮 + 导入弹窗（上传→统计→匹配预览，含「下载 CSV 模板」）。
8. 采购导入加「下载 CSV 模板」→ `purchases/page.tsx` 加 `downloadCsvTemplate()` + 文件选择区上方的下载链接。

## 已知待清理（重启后必做）
- 上次 tsc 报过 ~5 个 **TS6133 未使用变量**（删 isReadMode/按钮后 `apiPost`、`locale` 等可能成了未使用 import/参数）。逐个删未使用的 import/变量即可。`next build` 会因 TS6133 失败，必须清干净。
- 重点核查 `invoices/page.tsx`：上次怀疑「删 isReadMode state / 删 Mode 按钮」的两个 Edit 可能因中断未完全落盘（但也可能是读取乱码误判）。**重启后先确认这两处是否已生效**。

## 验证 & 部署
- `npx tsc --noEmit` 全过 → `npm run build` 全过。
- 提交 + 推送，走 GitHub Actions（`.github/workflows/deploy-*.yml`）自动部署，**不要用 gcloud builds submit**。
