# 会计核销重构 · 任务台账

来源：DEV-PLAN.md（2026-09-24 确认）。一周期 = 一个交付单元：读台账 → 做 → 验证 → 提交 → 回写状态。

- [x] 0. 风险核实：Trip COMPLETED 触发链路
      结论：Trip.status→COMPLETED 由通用 `PUT /api/trips/[id]`（不删）负责，`settlement/route.ts` 只读不写；
      提成冻结 recalcOrderCommission 有 4 条独立触发路径，全部与交账/对账 UI 无关；生产库 Trip.status
      从未出现过 COMPLETED、commissionFrozenAt 全部为空——这条链路本来就是死的。
      结论：司机端 `/classic/driver/settlement` 页面可以整页下线，不影响提成冻结。
      附带发现：lib/finance/statement.ts、app/api/analytics/logistics/route.ts、
      tests/driver-reconciliation.test.ts、tests/historical-debt-offset.test.ts、scripts/audit/**
      还有对 settlement 相关代码的引用，删除前逐个确认。

- [x] 1. Schema 迁移：Order.returnStatus + DriverCashConfirmation
      证据：20260924000001_order_return_status_and_driver_cash_confirmation 已 migrate deploy 到共享开发库，
      migrate status 显示 up to date；prisma generate 成功。

- [x] 2. 入账逻辑改造：lib/trip-settlement-payment.ts 支持 DRIVER_CASH_CONFIRM marker
      证据：postTripCollections→postCollections(marker通用化)，driverCashConfirmationMarker；
      同步改了 lib/finance/statement.ts、statements 路由/页面字段名、m03.ts 审计脚本；
      tests/trip-settlement-payment.test.ts + finance-statement.test.ts + historical-debt-offset.test.ts
      33 条全绿

- [x] 3. 新 API：/api/accounting/driver-cash（GET 汇总 + POST confirm）
      证据：两个路由已写好，tsc 通过；curl 探针留到任务 9 整体验证一起跑
      （现在权限点 finance.settlement.read/confirm 还没重新入种子，会先 403，
      待任务 5 完成后再测）
      依赖：任务 1、2

- [x] 4. 改造 /api/orders/bulk 的 mark_returned 三态
      证据：三态 + note + finance.write_off.confirm/.return 权限点判断已写好，tsc 通过；
      顺带修了 prisma/seed-events/events/sales.ts 里的 orderReturn 残留引用

- [x] 5. RBAC：finance.settlement 瘦身 + 新增 finance.write_off + route-map + 迁移
      证据：catalog.ts/seed-rbac.json/sortkeys.json/route-map.ts 都改了；
      20260924000002_accounting_writeoff_permissions 已 migrate deploy；
      顺带发现并修了 /api/orders/bulk 外层闸门漏挂 FINANCE 够不着的问题（否则
      finance.write_off.confirm 会是又一个摆设权限点）；无个人级例外挂在被收回的
      finance.settlement.create 上。测试/parity baseline 留到任务 7 删完旧路由后一并跑，
      现在跑会因为旧路由文件还在但 route-map 规则已删而出现预期内的临时 diff

- [x] 6. 重写 /classic/accounting 页面（钱板块 + 单板块两步核销）
      证据：拆成 page.tsx(orchestrator) + DriverCashBoard/WriteOffBoard/MissingReturnsQuery
      三个组件；单board今日订单改走 deliveryFrom/deliveryTo 精确查询，顺带修掉旧页面
      "默认500条createdAt倒序可能漏单"的问题；复用了 components/shared/sort-th.tsx
      而不是重复造轮子；tsc 通过。浏览器实测留到本周期末尾一次性做
      依赖：任务 3、4、5

- [x] 7. 下线司机交账/对账：删页面、删 API、删导航链接
      证据：删了 settlements/driver-reports 页面+API+组件+lib+测试+审计探针脚本(含
      package.json 里 3 条 test: 脚本)；finance/accounting/driver 三个 layout.tsx 导航链接已清；
      role-access.ts legacy 兜底名单同步更新；role-reachability.json + parity-baseline.json
      + rbac-legacy-token.test.ts 豁免表都已更新并写明理由；npm test 910/913 通过
      （1 个失败是 pricing-override.test.ts 缺 ABCT 客户测试夹具，与本次改动无关，
      2 个 skip 是同一夹具的连带）；tsc --noEmit 干净

- [x] 8. 司机端 /classic/driver/settlement 处理（视任务 0 结论而定）
      结论：整页 + DailyReportCard 组件 + 导航链接一并删除，任务 0 已确认不影响提成冻结

- [x] 9. 整体验证：tsc/build/migrate status/verify.sh 补充项
      证据：本地起 dev server，用 BOSS 账号(boss@demo.local)真实点了一遍浏览器：
      建了3个测试订单(1个挂司机)+1张POSTED发票 → 钱board正确显示Liam Walsh现金€123 →
      单board扫码核销TESTWO-B-001(→RETURNED)、退回核实TESTWO-C-001(→ISSUE+备注)、
      重新核对(→PENDING)、撤销(→PENDING)全部符合预期 → 钱board两步确认后
      真实生成Payment(€123)+Invoice推进PAID+DriverCashConfirmation落库 →
      重复POST confirm返回409且未二次入账(Payment仍1条) → 无token/错token探针
      均401。测试数据已清理，dev server已停。tsc/migrate status/build 见下方独立验证

- [x] 10. /code-review high 与 /security-review，处理 findings
      证据：security-review 未发现高置信度问题（新路由全用 Prisma 参数化查询、
      confirm 服务端重算金额+DB唯一约束保证幂等、/api/orders/bulk 内层仍分动作校验
      不存在越权、RBAC 授予精确匹配预期、无 dangerouslySetInnerHTML/eval）；
      code-review high 结果见下方，findings 逐条处理记录在 DEV-REPORT.md

- [x] 11. DEV-REPORT.md
      依赖：任务 10
