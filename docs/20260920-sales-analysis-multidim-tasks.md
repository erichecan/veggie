# Sales Analysis 多维度钻取改造 — 任务台账

> 20260920。需求来源：客户截图批注（`docs/shots/` 无原图，原件在微信）+ 用户口述。
> 台账是进度唯一真相，每周期回来先读这里。

## 需求原话（用户 20260920，未润色）

> 「前面我会选左边时间框架，右边是筛选，然后右边筛选能按照我刚发截图里的要求，
> 就是客户在绿色表格中点击按客户展开，还可以按按产品展开，是在客户这一层下面。」
> 「毛重去可售单位去去，没有的用商品基础 weight 兜底」

截图批注三条（客户方）：
1. 先按照客户展开，再按照产品展开
2. 展开顺序按照 司机 客户 产品
3. 横向加 毛重，CMS

箭头批注：多个日期行 →「都在一起显示」。
用户答疑确认：多选时间段**各自成组列出**；左边三个图标 ▤👥🚚 **去掉**，改用
演示页方案 B 那个**右面板顶部标签页**模式；CMS = **提成 Commission**。

## 已定的设计（不再反复问）

- 左表 = 时间骨架，月 › 周 › 日（20260920 已完成，见下）。**月/周/日三层都能勾选**。
- 日期行前三个图标去掉，改成勾选框。点行本身仍是展开/收起时间层级。
- 右面板顶部标签页选**第一层维度**，往下按固定顺序嵌套：
  | 标签 | 层级 |
  |---|---|
  | 司机 | 司机 › 客户 › 产品 |
  | 客户 | 客户 › 产品 |
  | 产品 | 产品 › 客户（保留 20260915 起就有的这条下钻） |
  | 业务员 | 业务员 › 客户 › 产品 |
  | 品类 | 品类 › 产品 |
- 右面板内容按选中的时间段**分组**，每组可折叠。
- 列加：毛重、提成。

## 生产数据实测（20260920，近 90 天 20249 行销售行）

⛔ 这两列做出来会是什么样，先有数：

| 指标 | 实测 | 含义 |
|---|---|---|
| 可售单位 `grossWeight` 有值 | 163 行 / 0.8% | 几乎没人维护 |
| 回落 `Product.weight` | 15382 行 / 76% | 实际靠它撑着 |
| 两者都没有 | 4704 行 / 23.2% | 毛重合计**系统性偏低** |
| 毛重按金额覆盖 | 74.3% | |
| 件提成 `commissionPrice` > 0 | 525 行 / 2.6% | |
| 订单 `commissionRate` > 0 | 81 单 / 2322 单 | |
| 订单 `commissionFixed` > 0 | **0 单** | |
| `deliveredQty` > 0 | 91.3% | 不是没送达导致的 0 |

→ 提成列在生产上 **97% 会显示 0**，是商品提成价/客户提成率没维护，不是功能坏了。
已在 20260920 告知用户。

## 任务单元

- [x] T0 月 › 周 › 日 三级钻取（先于本次需求做的）
      验收命令：见下「已验证」
      可看物：docs/shots/20260920-sales-analysis-monthly.png
      定性状态：已确认（用户看过截图后提了本轮新需求）
      证据：7月各周相加 30690.57+44264.04+572.05 = 75526.66 = 月合计；
            跨月第27周展开只出 07/01–07/05，日行相加 = 30690.57（不裁剪会是 48925.63）
      依赖：无

- [x] T1 后端：margin API 加 `driver` 维度（lib/analytics/pivot.ts DRIVER_NAME_EXPR/DRIVER_JOIN）
      证据：groupBy=driver 6 行相加 = 80798.55 = 不分组合计；口径与 driver-commission 同源

- [x] T2 后端：margin API 加 `driverName` 筛选参数
      证据：随 T6 嵌套下钻一起验（见 T6）

- [x] T3 后端：加 `grossWeight` 指标
      证据：8 个维度分组毛重相加均 = 21059.56（customer 因 round2 累积差 0.01）

- [x] T4 后端：加 `commission` 指标
      ⛔ 验证时踩坑：withCachedAuth 缓存导致首轮 8 个维度全返回 0，是**假阴性**——
         换日期范围绕开缓存 key 后才看到真值。缓存也是"度量工具自身失真"的一种。
      证据：本地库临时给订单 D153253 设 commissionPrice=0.5/rate=0.02/fixed=10（测完已还原并回读校验），
            lib/commission.ts 公式手算 64.50+10.00+24.51 = **99.01**；
            month/week/day/product/customer/driver/salesUser/category 八个维度
            Σcommission **全部 = 99.0100**，固定费分摊守恒。
      性能：生产 EXPLAIN ANALYZE（近 90 天 20045 行）改造前 191ms → 改造后 **441ms**，在 1s 阈值内。

- [x] T5 前端：左表去掉三图标、月/周/日加勾选框（多选）
      证据：浏览器实测 ▤👥🚚 已无；月/周/日三层都有勾选框，勾中行变绿

- [x] T6 前端：右面板改标签页 + 按时间段分组 + 嵌套展开（新文件 DimensionPanel.tsx）
      ⛔ 踩坑并已修：清空树和重新拉取放在**两个** useEffect 里会互相错过——
         补拉那个读到的是清空前的闭包值，判定"已有数据"就不拉了，表现为**换个标签页面板全空**。
         必须合并到同一个 effect，文件里已写明别再拆。
      证据：勾选 2026年7月 + 6月 → 面板两块（6 行 / 2 行）；
            司机 Conor Casey › 客户 Old Garden › 13 个产品，三级通；
            五个标签在 UI 上 Σ总额 全部 = €75,526.66 = 左表 7 月那行；
            Σ毛重 = 19449.51（客户维度 19449.52，round2 累积差 0.01）
      可看物：docs/shots/20260920-dimension-panel.png

- [x] T7 CSV 导出：左表导时间层级，面板自己导已展开的树（两个文件，列不同不混在一起）

- [x] T8 验收：tsc 干净；浏览器实测如上；排序见 T9

- [x] T9 排序（20260920 用户追加）：面板表头点击排序，**每一层都按同一规则排**
      证据：总额点一次 → 升序 97.85 < 179 < 266.90；毛利点一次 → 降序 1083.37 > 974.55 > 825.08；
            展开的子层跟着按毛利降序 264 > 70 > 67.44 > 46.80；表头箭头 ▼▲⇅ 随状态变
      顺带修：长商品名把行撑成 6 行高（实测 maxRowHeight 120px → **32.5px**），改截断 + title 看全名

## 已知问题（留着，没解决）

- **面板 7 列在 1500px 视口下仍会轻微裁到「毛重」列**。已给横向滚动兜底，
  左表也从 560px 收到 460px（面板 627px 刚好放下 620px 的表）。
  真要彻底解决得让面板可拖宽，或默认砍掉「未税」列（总额+毛利够用）——**等用户发话**，不自作主张砍列。
- 司机 key 用的是**司机名**：改名前后会变成两行（快照保留旧名）。与提成报表同病，本次未处理。
- margin SQL 已经很长（透视 + 非透视共用），再加指标前先考虑把 SELECT 列表抽常量。


## /code-review high 结果（20260920）

共 12 条。**4 条属于本轮改动，已全部修复并回归**；其余 8 条来自工作树里**早于本轮**的
未提交改动（打印模板 / 发票号那一批），本轮不动，已单列在下面交用户决定。

### 本轮的，已修

| # | 级别 | 问题 | 修法 | 验证 |
|---|---|---|---|---|
| 1 | HIGH | `DimensionPanel` 根缓存 key 不含当前标签，快速切标签时**上一个标签在途的响应会写进新标签**——表头写「客户」、内容却是司机名，再下钻会拿司机名当 customerId | `pathOf` 前缀加 `dim` | 连点「司机」→「客户」，表头=客户、首行=Copper Bistro（客户名）✅ |
| 2 | HIGH(性能) | 司机 JOIN 用 LATERAL 挂在**订单行**粒度，被求值 3 万多次 | 改成把 `orderIds` 一次 `unnest` 展开再普通 LEFT JOIN | 生产 400 天：**1194ms → 407ms**，12 个司机金额逐个一致 ✅ |
| 3 | MEDIUM | 按月点一次「显示更多月」= 24 个月 ≈ 730 天，撞后端 400 天上限被**静默截断**，最老那月只有半月数据却顶着完整月份标签 | 前端自己封顶，到顶把按钮换成说明 | 底部显示「已到分析范围上限（400 天）」✅ |
| 4 | MEDIUM | 勾选**当月/当周**时范围是整月，而左表只算到今天 → 面板合计大于旁边那一行 | 新增 `clampToToday`，勾选范围右端裁到今天 | tsc + 回归 ✅ |

⚠ 第 2 条 review 给的**根因是错的**：它推测 `PickingWave` 没有 GIN 索引导致每行 seq scan。
实测 `PickingWave` 只有 **76 行**，而且 Postgres 已经用 Memoize 把求值次数从 33 万降到 3.6 万。
真正的代价是「被求值 3.6 万次」这件事本身，不是单次扫描贵。所以解法是**消掉 LATERAL**，
不是加索引——加索引在 76 行的表上等于白加（记忆里「加索引不实测=加负担」那条）。

### 不是本轮的那 8 条 —— 用户 20260920 拍板「一并处理」，处理结果见下

| 级别 | 位置 | 问题 |
|---|---|---|
| HIGH | `app/api/orders/dispatch-print-data/route.ts:35` | **GET 请求会永久消耗发票号**。预览、取消打印、重试、浏览器预取都会发号；且 `ensureInvoiceNumber` 只查 `Order.invoiceNo`（:86/:89），**不查该单是否已被真实 Invoice 覆盖**，已开票的订单还会再占一个永远印不出来的号 |
| HIGH | `lib/print/invoice-lookup.ts:43` | 「真发票优先」那行会让 `Invoice.name` **覆盖**已经印给客户的 `Order.invoiceNo`：先印销售单给 V59086 → 财务后开 INV/2026/0123 → 重印变号，客户手上两张纸号不同 |
| MED | `lib/print/trip-sales-template.ts:165`、`classic/print/[id]/page.tsx:227` | `paymentLabel` 现在原样返回库里的自由文本，却**没有 escapeHtml**（同模板其他字段都转义了），含 `<`/`&`/引号会破坏单据 |
| MED | `lib/invoice-number.ts:58` | 发号时持 advisory lock 跑两次 14.8 万行的正则全扫，在 5s 事务上限内；批量打印时后来者可能 P2028 超时，而 `isUniqueViolation` 不认这个错 → 500 |
| MED | `classic/print/[id]/page.tsx:115` | 单单打印读 `order.invoiceNo` 但这条路径从不发号，同一单从不同入口打印，号/标签不一致 |
| MED | `operator/trips/page.tsx:317` | 客户签收单（合同第四条要求的 6 类单据之一）被整体删除，含 103 行测试，diff 里没有替代物 |
| LOW | `deploy/print-preview/Dockerfile:5` | `COPY .htpasswd` / `COPY site/` 的源已被 `.gitignore` 排除，干净检出构建不出来 |


## 打印/发票那 8 条的处理结果（20260920）

前置：读了 `docs/20260920-print-followup-tasks.md`，这批是同一天 T1–T6 一个完整工作流的产出。

| # | 级别 | 结论 | 做了什么 |
|---|---|---|---|
| 1 | HIGH | **部分修** | `ensureInvoiceNumbers` 三步走：已有号→原样返回；**已被真实发票覆盖→返回发票号且不写库**（原来会给它白占一个永远印不出来的号）；剩下的才发。<br>「GET 就发号」**保留**并写明理由：所有调用方（打印中心 3 个按钮 + 打印页本身）都是真打印意图，改 POST 要动 4 个调用方和两处权限表，收益不抵风险；代价是「打开预览又取消」会留号段空洞，号仍唯一且单调，不影响对账 |
| 2 | HIGH | **已修** | `loadInvoiceNoMap` 把「真发票覆盖自发号」改成「真发票只补空缺」。已经印给客户的号 sticky，不会因为财务事后开票就在重印时变号 |
| 3 | MED | **已修** | `trip-sales-template.ts` 两处 `${paymentLabel}` 加 `escapeHtml` |
| 4 | MED | **已修** | `classic/print/[id]/page.tsx` 两处同上（用 `lib/print/trip-common` 的 escapeHtml，该文件明确可供客户端组件使用）。转义只能加在**注入点**，不能加进 `paymentTermPrintLabel` —— 那个返回值还要给 React 渲染，预转义会显示成 `&amp;` |
| 5 | MED | **已修** | 批量发号改成**一个事务拿一次锁、算一次 max、连续发**（原来每单一次，20 单 = 20 次）；`Promise.all` 改顺序（同一个 interactive tx 上并发 Prisma 不保证）；批量事务 timeout 显式设 20s（默认 5s 按单张单定的，P2028 不是唯一约束冲突、重试认不出来会变 500） |
| 6 | MED | **不修，已注明** | review 基于死代码推测。实测**没有任何入口传 `?doc=sales`**（只有 `?doc=delivery` 和不带参数），该分支恒为 false；销售单实际走打印中心那条 `trip-sales-template`。已在代码里写明「将来加 doc=sales 入口必须同时补发号」及其两处权限登记要求 |
| 7 | MED | **误报** | 客户签收单是用户 20260920 **确认的计划内删除**（`docs/20260920-print-followup-tasks.md` T1），不是漏删。不恢复 |
| 8 | LOW | **已修** | 新增 `deploy/print-preview/README.md` 写清两个 COPY 源为何不在 git（.htpasswd 含口令哈希、site/ 是产物）及生成步骤；Dockerfile 顶部加前置说明。**不**把口令哈希提交进 git |

### 另一处 review 判断有误

第 5 条 review 称 `currentMax` 是「unindexable full scans」。生产实测（Invoice 148285 行 / V 格式 25223 行）：
第一条走 `Invoice_name_key` 的 **Index Only Scan，43.5ms**；第二条 **0.66ms**。
所以**没有**为它加表达式索引 —— 该省的是调用次数不是单次成本。

### 验证证据

发号行为（本地开发库，跑完自动还原并回读校验）：

```
① 批量发号        : V59091 V59087 V59088 V59089 V59086 V59090
② 幂等（号不变）   : PASS
③ 互不重复/号段    : PASS
④ 单个==批量       : PASS
⑤ 已开票不发新号   : PASS（返回 V57366，库里 invoiceNo 未变=null）
⑥ 重印不换号       : PASS（真发票 V57366 没有顶掉已印的 V999001）
↩ 还原后 invoiceNo : [null,null,null,null,null,null]
```

转义：`NET30 <b>urgent</b>` → `NET30 &lt;b&gt;urgent&lt;/b&gt;`；`A & B "terms"` → `A &amp; B &quot;terms&quot;`；
全仓 `grep '${paymentLabel}'` 无未转义残留。

测试：926 个用例 **923 通过 / 1 失败 / 2 跳过**。
失败的是 `pricing-override`「不传覆盖时使用客户档案默认价格表」—— 前置数据缺 ABCT 客户，
`docs/20260920-print-followup-tasks.md` 已记录它改动前同样失败，非本次引入。
⚠️ 另有一个**我引入**的测试失败已修：加 driver 维度后 `analytics-pivot.test.ts` 的
「DIMENSION_DEFS 覆盖 9 个维度」断言要改成 10 个。

### 本地开发库的一次性操作

为了能跑发号验证，把 `20260920000001_order_invoice_no`（纯加列 + UNIQUE 索引，不动数据）
应用到了**本地开发库**，并登记进 `_prisma_migrations`。生产未动。
这个迁移本来就会在下次部署由 `migrate deploy` 执行。


## 提交与部署（20260920）

- `b03e203` 打印单据 6 项改动 + 发票号两处高危修复
- `410d658` 销售分析：月/周/日三级 + 维度面板 + 毛重/提成/单位三列

### 单位（unit）列 —— 用户 20260920 追加需求

两处都加了：产品明细面板（产品名后，可排序）与明细视图（产品后），CSV 同步。

⛔ **不能假设「一个产品一个单位」**：生产实测 6227 个产品×天分组里 345 组（**5.54%**）
同时用了多种单位，所以是 `string_agg` 不是 `MAX`。实测输出：
- 单一单位：`Spanish Onion → BAG`、`Tomato Beef CASE → CASE`（与客户截图里的产品对得上）
- 多单位：`Fresh Red Chilli-3 → 1KG, 1公斤, 500g, CASE`

⚠️ 顺带暴露一个**主数据问题**：`1KG` 和 `1公斤` 是同一个单位的中英文两条记录。
显示是对的，脏的是数据。要不要合并待用户决定。

空 uomName（生产 3.8%，20260823 之前的历史行）按 `displayUomName` 口径显示 `⚠ Unit(s)`。

### 提交时的两处阻碍

1. pre-commit 拦下预览脚本里硬编码的一个**客户真实域名的账号**（不在这里复述，否则这份文档自己又会被拦）。
   **没有用 --no-verify 绕过**，改成必须由环境变量提供。
2. 另一个会话在同一工作目录并发作业（采购分析）。逐个文件 add + worktree 干净验证，
   没带走对方的 `next.config.ts` 与 `boss/reports/*`。做法已补进记忆
   `parallel-fork-git-stash-race-20260904`。

### 干净树验证（git worktree 检出 410d658，symlink node_modules + lib/generated）

- `npx tsc --noEmit` 0 错
- 926 用例：923 通过 / 1 失败（pricing-override 缺 ABCT 前置数据，改动前同样失败）/ 2 跳过
- 权限安全绳（role-reachability 等 5 个）**52/52 全绿**
- CI 在 main 上 success
