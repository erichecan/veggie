# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# 本地数据库备份（维护脚本生成，不入库）
/backups/

# Odoo 源数据导出（真实客户/订单 PII，绝不入库）
/odoodata/

# 20260816 合规审查发现 pic/ 下混有 Odoo 真实客户导出（res.partner/sale.order 等 csv、
# 发票 PDF）及历史备份子目录，曾被公开仓库暴露（c-d17237）。此目录只允许存放脱敏后的
# UI 截图素材；任何 csv/xlsx/xls/sql/pdf 一律禁止入库，需要新增示例数据请用 /pic/samples/
# 之类目录并确认已脱敏。
/pic/**/*.csv
/pic/**/*.xlsx
/pic/**/*.xls
/pic/**/*.sql
/pic/**/*.pdf
/pic/backup-*/

# 演示种子数据 manifest（记录生产库真实 id，本地状态，不入库）
scripts/.demo-seed-manifest.json

# 迁移脚本写入前的整表快照备份（含真实客户/商品 PII，仅用于本地回滚参考，不入库）
scripts/.backup-*.json

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/lib/generated/prisma
.playwright-mcp/
*.png

# graphify 知识图谱产物（本地生成，含 4.3MB graph.html）
graphify-out/

# 服务器凭据（含 SSH 私钥），严禁入库
docs/dev-server-info/

# Office 打开文档时生成的锁文件（~$开头）。20260807 发现有一个被 git add -A 扫进了
# 提交历史，而它在 Word 关掉时就消失了 —— 只会留下一条莫名其妙的删除记录。
~$*
