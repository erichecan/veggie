# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# 本地数据库备份（维护脚本生成，不入库）
/backups/

# Odoo 源数据导出（真实客户/订单 PII，绝不入库）
/odoodata/

# 演示种子数据 manifest（记录生产库真实 id，本地状态，不入库）
scripts/.demo-seed-manifest.json

# 迁移脚本写入前的整表快照备份（含真实客户/商品 PII，仅用于本地回滚参考，不入库）
scripts/.backup-*.json

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/lib/generated/prisma
.playwright-mcp/
*.png

# graphify 知识图谱产物（本地生成，含 4.3MB graph.html）
graphify-out/
