# DEV-REPORT — 商品基准单位/采购单位/可售单位换算与定价

> 完成日期：2026-08-23
> 对应计划：`DEV-PLAN.md`

---

## 做了什么

商品详情页「可售单位」区块过去有三个问题：基准单位能从两个入口改、系数要用户自己手算、独立售价只有"留空自动/填了就固定"两种状态。这次把三处都收拢/补齐了：

1. **基准单位改成单一入口**：只有页头「Unit of Measure」下拉框能改基准单位。下面「可售单位」区块的"基础"不再是能点的单选钮，改成纯展示——哪一行的单位等于页头选的那个，哪一行就自动显示"基础"徽章、系数锁定为 1。保存时服务端也是按这个口径重新计算，不再信任前端传来的每行 isDefault；如果提交的行里没有一行对得上当前基准单位，服务端会自动补一行（系数 1），不用先手动把基准单位加进列表才能保存别的行。
2. **采购单位跟着采购单走**：「确认采购」这一步（原本就会顺带生成供应商草稿账单）现在还会把这张单里每个商品最后一次出现的采购单位，回写到商品的「Purchase UoM」字段。只影响这次改动上线之后新确认的采购单，历史数据不动。
3. **换算计算器**：系数输入框旁边加了「🔧 帮我算」按钮，两种模式：
   - **按重量**：填个数量（克/公斤），配合商品「其他信息」里的净重（Net Weight），自动算出系数（要求净重必须先填，没填会置灰提示）。
   - **按已有单位的倍数**：比如已经填了"1 小袋 = 0.05 基础"，这里选"参照小袋"、填倍数 5，直接算出"5 袋装 = 0.25"，不用自己心算，也不容易算错方向。
   两种模式都只是帮你把系数算对、填进原有的输入框，不新增任何持久化字段。
4. **价格公式面板**：每行价格从单一输入框改成"自动 / 固定 / 公式"三态。自动=基准价×系数（现状不变）；固定=独立售价（现状不变，只是显式建模）；公式=新增，"基准×系数"这一步之后再打折扣%、加减一个金额，实时算出最终价格。

## 涉及文件

| 文件 | 改动 |
|---|---|
| `prisma/schema.prisma` + 新迁移 `20260823000002_product_sale_uom_price_mode` | `ProductSaleUom` 新增 `priceMode`（枚举 AUTO/FIXED/FORMULA）/`priceDiscountPct`/`priceSurcharge`；存量行按 `priceOverride` 是否有值一次性归类为 FIXED，其余是 AUTO，计算结果一个数字不变 |
| `lib/sale-uom.ts` | 新字段类型与校验；`priceOf()` 按 `priceMode` 三态分支计算（唯一计算口径，下单/报价/订单详情三处调用点不用改代码） |
| `app/api/products/[id]/sale-uoms/route.ts` | 删掉"可售单位保存时覆盖模板销售单位"的逻辑，改成读模板 `uomId` 派生每行是否基础；接住并落库价格公式三个新字段 |
| `app/api/product-templates/route.ts` | 新建商品时，基础行同样按页头选的 `uomId` 派生（新建场景保险对齐） |
| `app/api/purchase-orders/[id]/route.ts` | 「确认采购」时按行 `uomId` 回写商品 `purchaseUomId` |
| `app/[locale]/classic/operator/products/[id]/page.tsx` | 可售单位区块重做：基础行改纯展示、加换算计算器、加价格公式面板；页头 Unit of Measure 旁加换基准单位的提醒文案 |

## 验证结果

在本地开发环境（连的是 Neon 开发库，不是生产库）里用真实浏览器操作 + 直接调接口两种方式验证，用的是「Vannamei Shrimps…6*2KG CASE」这个真实商品。

| 验证项 | 方式 | 结果 |
|---|---|---|
| 基准单位单一入口：把某行单位选成和页头一致 | 浏览器操作 | ✅ 自动显示"基础"徽章，系数锁定显示 1，价格变成基准价的只读展示，不再能编辑 |
| 保存时没有基础行会被自动补 | 浏览器操作：只配了 PKT/PACK 两行，未手动加 CASE 行就保存 | ✅ 服务端自动补了一行 CASE（isDefault=true, factor=1），三行一起落库 |
| 换算计算器·按重量 | 净重填 12kg，输入 300g | ✅ 算出系数 0.025，人话提示"约等于 1 基础单位可拆成 40 份"，点应用后系数框正确写入 0.025，自动价 €85×0.025=€2.13 |
| 换算计算器·按倍数 | 参照上面的 PKT（×0.025），填倍数 5 | ✅ 算出系数 0.125（=5×0.025），应用后自动价 €10.63 |
| 价格公式·固定 | 把 PACK 行切到"固定"，填 €9.99 | ✅ 保存后 GET 返回 `priceMode:"FIXED", priceOverride:9.99`，刷新页面查看态显示"€9.99 Fixed"只读 |
| 价格公式·公式 | 折扣 10% + 加价 €0.5，基于 €2.13 | ✅ 实时算出最终价 €2.41（=2.125×0.9+0.5，四舍五入），保存后 GET 返回三个字段落库正确 |
| 价格公式·服务端校验 | 直接调接口传折扣 150% | ✅ 400，"折扣百分比必须在 0–100 之间" |
| 换基准单位后旧系数归一（边界情况，非计划原文但测试中发现并修复） | 把一行系数设成 0.025 后又把它的单位改成和基准一致 | ✅ 保存不再报错，服务端/客户端都会把变成基础的那一行系数强制归 1（发现过程见"已知问题"） |
| 采购单位跟着采购单走 | 给一张 DRAFT 采购单的行设置 uomId=CASE，调用「确认采购」 | ✅ 商品模板 `purchaseUomId` 从 `null` 变成 CASE 对应的 uomId |
| 存量数据不受影响 | 检查未触碰的其它可售单位行 | ✅ `priceMode` 全部按迁移脚本正确回填为 AUTO/FIXED，数值未变 |
| 编译/构建 | `npx tsc --noEmit`、`npm run build` | ✅ 全部通过，无新增报错 |

## 已知问题（开发过程中发现，已修复）

验证换基准单位场景时发现一个真实 bug：把某一行的单位改成和基准单位一致后，这一行原来的系数（比如 0.025）没有跟着归一，直接保存会被后端校验拦下「默认单位就是库存的计数单位，换算系数必须为 1」——这正是计划里提过的"换基准单位后系数需要重新核对"场景，但当时的实现是报错阻塞而不是静默归一。已在服务端（`/api/products/[id]/sale-uoms`、`/api/product-templates` 两处）和客户端都补上：变成基础行的那一行系数自动归 1，不会再拿一个用户没机会去改的旧数字挡住保存。

## 已知限制（不在本次范围，供参考）

采购单详情页目前只有"新增行"能设置 `uomId`，编辑已有行不会改这个字段（`PUT /api/purchase-orders/[id]` 的既有行更新逻辑本来就没带这个字段）。这不影响「确认采购回写 purchaseUomId」这条新逻辑本身（只要行上有 uomId 就会生效），但如果客户想改一张草稿采购单里已有行的采购单位，目前做不到，只能删了重加。这是发现的既有缺口，与本次三个需求无关，按计划风险点#4 的原则没有顺手修。

## 下一步

无阻塞项。如果客户后续反馈"想改已有采购单行的 uomId"，可以再排期补上。

本地开发服务器：http://localhost:3000
