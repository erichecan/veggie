# DEV-REPORT — 可售单位提成价按单位换算

> 完成日期：2026-09-01

## 做了什么

你反馈：商品在基础单位上原本有一个给司机的提成价，加了"可售单位"（比如按箱、按整托卖）之后，提成价也应该像售价一样跟着单位换算。你选定的方案是"照抄价格的完整机制"——每个可售单位都能像配置价格一样，单独设提成的一口价（FIXED）或折扣/加价公式（FORMULA），不设置就按换算系数自动折算（AUTO，跟改造前的效果一致）。

### 核心改动

- 商品的"可售单位"表新增 4 个字段，跟价格那边的字段一一对应：独立提成价、计算模式（AUTO/FIXED/FORMULA）、公式折扣百分比、公式加减金额。
- 新增一个提成价换算函数，逐字镜像现有的价格换算函数（同样的三种模式、同样的取整规则），保证两边的算法此后不会走偏。
- 下单、报价单、订单里加行/编辑行，全部改成用这套换算算出"这一行在它选用单位下应得的提成价"再落库——跟单价走的是同一套服务端权威定价流程，不再是"下单时抄一遍基础提成价，之后再零散地按数量折算"这种两步拼接的老办法。
- 司机提成报表（SQL 聚合那份）原来自己重复实现了一遍单位换算，现在提成价落库时已经算好，删掉了这份重复实现，两边少了一处"必须靠人工记着保持同步"的地方。
- 存量历史订单行（生产库上实测只有 1 条符合条件）已经在数据库迁移里一次性回填过换算后的数值，不用你另外操心。
- 商品编辑页的"可售单位"区块，每个非基础单位行下面新增一行"提成"公式编辑器，跟价格那行长得一样、算法也一样。

### 验证过什么

- 34 条新增单元测试全部通过（提成换算的三种模式、边界值、校验规则），加上全仓库原有 797 条测试跑了一遍，除了一条跟本次改动无关的既有失败（缺一份本地没有的测试客户数据，这个 test 依赖生产库快照，不在本次范围内）全部通过。
- 直接调用服务端定价引擎，构造了一个"基础单位 + 一口价箱装 + 折扣加价公式"的商品，验证算出来的提成价和最终写入订单、汇总求和的结果都对得上手算的数字。
- 在本地起了一个独立的开发服务器，登录后台真实打开商品编辑页、切到编辑态、改了一处提成公式的折扣值，点保存，确认接口返回成功、数据库里的值确实按新公式更新了（过程中还顺手发现并修了一个提成校验范围的小 bug，见下方"顺手修的问题"）。
- 用一条真实生产历史订单行核对过：数据库迁移的回填计算（基础提成价 €0.8 × 换算系数 0.025 = €0.02）跟生产库里的实际记录完全对上。

### 顺手修的问题

写完 UI 后用真实浏览器点了一遍，发现"提成折扣百分比"的校验范围写成了 0–100（只许打折不许倒扣出负数），但界面上的输入框本身明明允许填负数（负数=打折，这是价格公式那边已有的界面提示，我原样照抄了过来）。会出现"界面让你填负数，一提交就报 400"的死锁。已经把范围改成 −100–1000（−100 对应"提成直接归零"，再往下没意义），并补了对应的测试用例。

## 涉及的文件

| 类型 | 路径 | 说明 |
|---|---|---|
| 数据库迁移 | `prisma/migrations/20260901000001_sale_uom_commission_price` | 新增 4 字段 + 存量数据回填 |
| 核心逻辑 | `lib/sale-uom.ts` | 新增 `commissionPriceOf()` 换算函数 |
| 核心逻辑 | `lib/server-pricing.ts` | 下单定价引擎顺带算好每行的提成价 |
| 核心逻辑 | `lib/commission.ts` | 提成汇总不再需要二次按数量折算 |
| 核心逻辑 | `lib/analytics/driver-commission.ts` | 删掉报表里重复的单位换算 SQL |
| API | `app/api/orders/route.ts`、`app/api/orders/[id]/route.ts`、`app/api/orders/[id]/lines/route.ts` | 建单/编辑/加行都改用新的换算结果 |
| API | `app/api/products/[id]/sale-uoms/route.ts` | 保存商品可售单位时接受新字段 |
| 页面 | `/classic/operator/products/[id]` | 可售单位区块新增提成公式编辑器 |

## 已知不受影响 / 不在本次范围

- 没配置可售单位、或没给某个单位单独设提成的商品，行为跟改造前完全一样（提成价 = 基础提成价 × 换算系数），不用任何操作。
- 只改了"提成价怎么按单位换算"，司机提成体系本身的其它规则（客户提成率、固定辛苦费、冻结时机）都没有动。
