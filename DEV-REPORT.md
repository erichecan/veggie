# DEV-REPORT：销售/采购数据分析扩展（7 条需求）

对应 `DEV-PLAN.md` + 台账 `docs/20260915-sales-procurement-analysis-tasks.md`（T1-T17 全部完成）。

## 背景

客户口述了 7 条具体的数据分析诉求（4 条销售 + 3 条采购）。先核实了现有的"销售钻取"
（09-13 晚上做的）和"采购运营/采购分析"这几个页面，发现都只能部分满足，缺口包括：
时间粒度缺季/年、筛选只能选一个客户/产品（不能多选几个对比）、毛利这一列有的页面是
写死返回 0 的假数据、库存趋势(on-hand+预测)完全没有数据基础。这次在"销售钻取"页面
的交互框架基础上做了扩展，同时新建一个"采购进货分析"页面。

## 做了什么

**销售侧（对应需求1-4）**：
- 时间维度新增按季/按年（原来只有周/月），加了自定义时间区间选择
- 新增"按产品""按客户"两个维度切换，都能叠加多选筛选（原来只能选一个客户/产品）
- 指标补全：数量/均价/未税金额/税额/含税总额/毛利/毛利率，一次性全部显示（不用像
  以前那样一次只能看一个指标）
- 新增"明细"视图：选定时间段+几个客户+几个产品，直接看逐笔订单明细（日期/客户/
  产品/单价/数量/金额），不聚合
- **顺带修复**：原页面"饼图/柱状图"两个视图的毛利/折扣两列一直是写死的假数字（代码
  里 `return 0`），跟真实数据完全不符；这次砍掉了这两个视图，改成统一走真实数据的
  表格，不会再看到假数字

**采购侧（对应需求5-7，新页面"采购进货分析"）**：
- 选一家供应商，看这段时间从它进了哪些货、数量、金额（需求5）
- 选几个产品，看这段时间的进货情况（需求7）
- 选几个产品，按周或按月展开，看 on-hand（当前库存）和"出货量趋势"（需求6）

## ⚠️ 需要你知道的一个口径问题

需求6要的"forecast QTY"，现有系统只能算出"当前这一刻"的库存预测，没有"未来"这个
维度——不是这次没做完，是这个数字本来就不存在按周/月展开的算法。跟你确认过之后，
这次用"该周/月实际卖出去的数量"来代替，帮你看出货趋势、判断要不要囤货，**不是真正
的需求预测算法**（不会自动推算未来）。如果以后需要真的预测（比如按历史规律推算下
个月大概要卖多少），需要另外讨论算法，工作量会更大。

## 页面

| 页面 | 路径 | 说明 |
|---|---|---|
| 销售钻取 | `/classic/boss/sales-analysis` | 已有页面，本次扩展 |
| 采购进货分析 | `/classic/boss/procurement-analysis` | 新页面，导航已挂 |

两个页面入口都在「经营总览」左侧导航栏，登录 BOSS 角色账号即可看到。

## 功能完成度

| 需求 | 完成度 | 说明 |
|---|---|---|
| 1. 按时间维度(周/月/季/年)+多产品+多客户+全指标 | ✅ | |
| 2. 按产品维度+多客户 | ✅ | |
| 3. 按客户维度 | ✅ | |
| 4. 明细点货清单 | ✅ | |
| 5. 按供应商查进货 | ✅ | |
| 6. 按产品看销售+on hand+forecast 周/月展开 | ⚠️ | forecast 口径见上方说明，不是真预测算法 |
| 7. 按产品查进货 | ✅ | |

## 验证结果

无新增数据库变更，全部基于现有表/视图运行时聚合。以下均在本地开发环境用真实业务
数据核实通过：

| 项目 | 方式 | 结果 |
|---|---|---|
| 类型检查 | `tsc --noEmit` | ✅ 全程无报错 |
| 生产构建 | `npm run build` | ✅ 一次编译成功 |
| 新接口鉴权 | curl 无 token | ✅ 401 |
| 多选筛选向后兼容 | curl 单客户 vs 双客户 | ✅ 金额从 €9713.6 正确翻到 €18264.45 |
| 明细清单边界 | curl 2客户×2产品组合 | ✅ 精确收窄到 7 行 |
| 库存趋势边界 | curl 不传产品/超10个产品 | ✅ 均正确返回 400 |
| on-hand 重建正确性 | 手工查 StockMove 原始记录核对 | ✅ 唯一一条 +260.344 记录，bucket 边界前后 on-hand 从 0 跳到 260.344，与手算完全一致 |
| 销售钻取页面全部交互 | Playwright 真实登录浏览器操作 | ✅ 三维度四粒度切换、多选筛选、明细清单、按周钻取(原功能)均正常，控制台无报错 |
| 采购分析页面全部交互 | Playwright 真实登录浏览器操作 | ✅ 按产品/按供应商切换、供应商筛选(金额€8033.20与独立curl验证一致)、库存趋势周/月粒度切换均正常 |
| 生产部署 | GitHub Actions + SSH 核实容器镜像 tag | ✅ 容器运行最新提交镜像，状态 healthy |

## 已知不可用功能 / 限制

- 需求6的 forecast 是"历史实际出货量"近似值，不是预测算法（已在页面上用黄色提示条标注）
- 库存趋势一次最多同时看 10 个产品（超过会被接口拒绝，避免拖垮生产库）
- 明细清单一次最多返回 2000 行，超出会提示"结果过多"并截断（页面上有黄色提示）

## 已知历史问题（本次顺带发现，未修复，留给下一轮）

- `procurement-analysis` 的供应商/产品筛选用的是新搜索接口，跟老的"采购运营"/
  "采购分析(报表)"两个页面是独立实现，三者数字口径大体一致但没有强制对齐测试，
  以后如果三处数字对不上，要单独排查
