# DEV-REPORT — 账期灵活化 + 商品/定价一批修复

> 完成日期：2026-08-26

## 做了什么

客户反馈"账期到了太死板，希望会计能灵活延长账期"，排查后发现现有账期体系比预想粗糙（逾期检测漏了大半客户），顺带查出好几个客户在实际操作中截图反馈的商品/定价 bug。这次一并做完了，分 5 个模块，全部本地验证过、部署到了生产。

### 模块 A：商品选择器缓存刷新
客户反馈"改了商品又去下单，选到的还是旧数据"。查明是打开选品下拉框时不会强制刷新商品列表（只在打开页面/切回浏览器 tab 时刷新，节流 30 秒），编辑商品和测试下单如果在同一个浏览器会话里紧挨着做，就会读到旧数据。现在打开选品框会强制拉一次最新数据。顺带修了切换行内单位后价格来源标签不同步的问题。

### 模块 B：加商品重复行
客户截图显示同一商品出现两行、价格还不一样。原因是选中一个已经在单子里的商品会无脑新增一行、重新算一次价。现在改成：选中已存在的同商品同单位行，直接数量+1；商品相同但单位不同（比如同时买 2箱+5公斤）仍正常分两行，不会被误判成重复。

### 模块 C：服务端定价公式对齐
排查客户反馈的一个价格误差时，发现服务端"权威定价"和前端预览用的是两份不同公式——服务端算多规格换算价时漏了加价/折扣这部分，导致操作员看到的预览价和保存后的价格对不上，正确的价格反而被系统当成"异常"打回旧值。已改成两边共用同一份公式。

### 模块 D：价格表页面显示实际售价
价格表管理页里配了折扣/加价公式的商品，之前只显示"5% 折扣 + 加价 1.0 欧"这种公式文字，运营看不出实际卖多少钱。现在旁边直接算出"≈ €19.50"显示出来。

### 模块 E：账期灵活化 + 会计延期审批（这次的主线需求）
- 账期从"现结/周结/月结"三档扩到五档：现结、周结、双周结、月结、双月结
- **修了一个真实存在的漏洞**：逾期检测原来只查月结客户，现结/周结客户欠款逾期了系统完全不知道，现在统一检查
- 会计/主管在客户详情页能给逾期客户批"延长账期"（+1周 / +2周 / 自定义天数 + 备注），延期期间该客户下单不会被信用问题卡住，但欠款/逾期金额还是如实显示，不会被藏起来
- 开票时如果没有手动填到期日，系统会按客户账期自动推算（发现这个字段之前一直是空的，逾期检测因此形同虚设）
- 每次延期都留痕（谁批的、批了几天、为什么），不是像以前那样"BOSS/FINANCE 角色随便绕过"这种查无实据的口子——那个应急绕过口子本身还留着，两边并存

## 页面/接口清单

| 类型 | 路径 | 说明 |
|---|---|---|
| 页面 | `/classic/operator/customers/[id]` | 新增账期五档下拉 + "延长账期"面板 |
| 页面 | `/classic/operator/place-order`、`/classic/operator/quotations/[id]` | 信用面板加"账期已临时延长至 X"提示 |
| API | `POST /api/customers/[id]/term-extension` | 新增：批延期，需要 `master.customer.extend_term` 权限（发给 BOSS/FINANCE） |
| API | `GET /api/customers/[id]/credit` | 改用共享校验逻辑，返回加了延期状态字段 |
| API | `POST /api/orders` | 信用拦截改用共享逻辑，覆盖所有账期类型 |
| API | `POST /api/invoices` | dueDate 未传时按账期自动推算 |

## ⚠️ 需要你知道的一件事

排查时用生产库核实发现：现在的逾期拦截判断依据的是 2026-07-18 那次 Odoo 迁移导入的历史快照——**本月及此后 Invoice/Statement/Payment 三张表新增记录数都是 0**，也就是说这套自动拦截其实吃的是一份从没更新过的旧数据，不管客户是不是已经把欠款还清都不知道。这次做的"会计延期"是在这份可能已过时的判断上给会计一个手动、留痕的豁免入口，不是把整条开票/收款流程修好——那是另一个更大的项目，这次没碰。如果你们线上开票、收款走的是另一套系统/流程，这点尤其要注意：新做的自动到期日推算只对将来在这个系统里"真正开票"的数据生效。

## 验证结果

本地起 dev 服务，用真实登录 + curl/浏览器（Playwright）逐条验证：

| 场景 | 验证方式 | 结果 |
|---|---|---|
| 同商品选两次，同单位 | 浏览器实测（place-order 页） | ✅ 合并成一行，数量 1→2，无重复提示 |
| 服务端算价对齐前端预览 | 真实 POST /api/orders | ✅ 修复前算漏加价被打回旧值，修复后接受正确价 |
| 价格表 formula 行显示实际售价 | 浏览器实测 | ✅ "≈ €19.50" 与 Cost/Public Price 手算一致 |
| weekly 客户逾期检测 | 真实 API 调用 | ✅ 改造前不会拦，改造后 `canOrder:false`（漏洞已堵上） |
| OPERATOR 调延期接口 | 真实 API 调用 | ✅ 403，权限门禁生效 |
| FINANCE 账号批延期 | 真实 API 调用 + 浏览器点击 | ✅ 200，落库审批记录，客户详情页/下单页同步显示延期状态 |
| 延期后欠款是否被隐藏 | 真实 API 调用 | ✅ 欠款数字仍如实返回，只是不拦 |
| 开票不传到期日 | 真实 API 调用 | ✅ 按客户账期自动推出正确日期 |
| RBAC 安全绳测试 | `npm test` | ✅ 84/84 通过（含新路由的可达性快照更新） |
| 全量测试套件 | `npm test` | ⚠️ 779 个里 1 个失败，是共享开发库缺一个叫 ABCT 的测试客户数据，跟本次改动无关（既有环境缺口，未处理） |

## 已知不可用/未处理的问题

- 商品搜索下拉里"候选项灰置、刷新也没用"这一条客户反馈过，代码里没找到对应的禁用渲染逻辑，怀疑是浏览器缓存或误认，没有强行给一个"修复"了事——如果客户还遇到，需要现场复现才能进一步定位
- 见上面"需要你知道的一件事"：开票/收款流程本身处于停摆状态，不在本次范围
- 账期天数按固定自然日算（7/14/30/60），不按自然月对齐——如果你们对账单是按每月固定几号出，需要另外调整

## 部署

已推送 main，6 次 GitHub Actions 部署全部成功，生产站点 `https://www.johnstonebros.ie/` 已是最新代码。

台账：`docs/20260826-account-terms-and-pricing-fixes-tasks.md`
